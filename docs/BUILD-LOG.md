# Build log (archived)

> **This is the historical build record, kept for reference — it is no longer the file to read first.**
> The whole numbered build (Steps 0–12) and the post-1.0 backlog (PI-1 … PI-74) below are **done and browser-verified**. For current status and the next actual work, read [`ROADMAP.md`](../ROADMAP.md) — it carries only what's still open or awaiting a live browser-verify. Full design rationale lives in [`PLAN.md`](../PLAN.md). This file stays as the detailed "how each piece was built and verified" log.

Granular checklist. Full rationale for any step lives in `PLAN.md`.

**Status: Steps 0-11 done — the entire local build is complete. Only Step 12 remains (deploy to DaxLite), which Tobias runs himself.**

**Privacy correction (2026-08-24, post-completion):** real tournament data (friend-group names, results) was flagged in Step 11 as a decision for Tobias — he decided it should never have been in the repo at all. Fixed: `import-legacy.ts` no longer compiles the data file in (reads a runtime path/env var instead — see README's "History import" section), the real data now lives in `legacy-data.local.json` (gitignored, kept locally for his own deployment), and every real name/result was scrubbed from `PLAN.md`, `STEPS.md`, the two test files, and the README screenshot (pseudonymized or genericized, methodology kept, identities removed). Git history was rewritten (`git filter-repo`) and force-pushed to remove the real data from every past commit too, not just going forward.

**Housekeeping (2026-08-24):** Tobias asked whether the repo was actually clone-and-continue ready on a different machine — verified rather than assumed, by doing a real fresh clone from the Forgejo remote and running the documented `docker compose up` from scratch (worked). That check surfaced two real gaps, both fixed and re-verified: (1) the README's status section was badly stale, still describing pre-Step-9 state; (2) the documented `npm run dev:server` local-dev path didn't actually work — no `DATABASE_URL` was ever available outside Docker, and the `dev` script didn't load `.env` at all. Fixed by publishing the `db` service's port in `docker-compose.yml`, adding `--env-file=../.env` to the server's `dev` script, and adding a working local-dev `DATABASE_URL` to `.env.example`. Re-verified the fixed path end-to-end (`docker compose up -d db` + `npm run dev:server` genuinely boots and serves `/api/healthz` against real Postgres), not just that it looked more complete.

## Step 0 — Project bootstrap ✅
- [x] Create `~/Projects/LimitedGauntlet/`, `git init`
- [x] Write `CLAUDE.md`
- [x] Write `PLAN.md` (incl. historical reference data transcribed from Outline)
- [x] Write `STEPS.md` (this file)
- [x] Add `~/Projects/LimitedGauntlet/CLAUDE.md` line to claude-memory's `projects.md` "Linked CLAUDE.md files" section
- [x] First commit (`407c608`)

## Step 1 — Repo scaffold ✅
- [x] `package.json` workspace layout: npm workspaces, `server/` + `client/`
- [x] `server/`: Fastify app skeleton, TypeScript strict config, `/api/healthz` endpoint (queries the DB, not just a static 200 — proves the Prisma connection works too)
- [x] `server/prisma/schema.prisma`: initial schema per PLAN.md's data model section (Organization, OrganizerAccount, Player, Tournament, TournamentPlayer, Pod, Team, TeamMember, Entrant, Round, Match, CardPull)
- [x] First Prisma migration (`20260823205506_init`), verified applies cleanly via both `migrate dev` (against a live diff) and `migrate deploy` (the production/entrypoint path) against a fresh Postgres
- [x] `client/`: Vite + React 19 + TS strict + Tailwind v4 skeleton, placeholder page that calls `/api/healthz` through TanStack Query to prove the front-to-back wiring works
- [x] `Dockerfile`: multi-stage (deps → client-build / server-build → runtime), builds clean, final image serves the SPA + static assets + `/api/healthz`
- [x] `docker-compose.yml` + `.env.example`: `app` + `db` (postgres:16-alpine), healthcheck-gated startup order
- [x] `docker/entrypoint.sh` runs `prisma migrate deploy` before starting the server
- [x] Verify: `docker compose up` from a clean checkout (fresh `.env` from the example, no manual DB steps) — confirmed migrations auto-apply, `/api/healthz` returns `{"status":"ok"}`, SPA HTML + JS/CSS assets all serve with 200s. Torn down cleanly after.
- [x] Security: caught and fixed a path-traversal vulnerability in the pinned `@fastify/static` version during `npm audit` (was `^8.0.4`, bumped to `^10.1.3`) before it ever ran; also overrode a transitive `deepmerge-ts` vuln pulled in by Prisma's dev-only config loader (`"overrides": {"deepmerge-ts": "^8.0.0"}` in root `package.json`) — verified the override doesn't break `prisma generate`/`migrate`. `npm audit` is clean (0 vulnerabilities).
- Note: evaluated Prisma 7 (latest stable) but it moves `datasource.url` out of `schema.prisma` into a new `prisma.config.ts` + driver-adapter system that's still quite fresh — reverted to Prisma 6.19.x (latest 6.x, still receiving fixes) to keep the stack boring and approachable. Revisit this decision once Prisma 7's config system has matured, not before.

## Step 2 — Auth + multi-tenancy ✅
- [x] `POST /api/auth/signup`: creates Organization + first OrganizerAccount together in one Prisma transaction (no dangling org if account creation fails), zod-validated, argon2 password hashing, sets session on success
- [x] `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`
- [x] `@fastify/secure-session` cookie sessions — key from `SESSION_SECRET` env var (32-byte hex, fails fast at boot if missing/wrong length), `SESSION_COOKIE_SECURE` env var to control the cookie's `Secure` flag (default off so local/LAN HTTP use isn't silently broken; flip on behind a TLS reverse proxy)
- [x] `requireAuth` preHandler hook (`server/src/auth/middleware.ts`): loads the organizer from the session, attaches `request.organizer = {id, orgId, email, name}`, 401s otherwise. This is the entire multi-tenancy boundary — every org-scoped route filters by `request.organizer.orgId`
- [x] `Player` roster CRUD (`GET/POST/PATCH/DELETE /api/players`) — org-scoped; update/delete use `updateMany`/`deleteMany` with `{id, orgId}` in the `where` clause (not a plain `update`/`delete` by id) so a request can never touch another org's row, by construction not just convention
- [x] Verify: full live exercise against a real running stack (not just reasoning about the code) — created two separate orgs, confirmed each only lists their own players, confirmed Org B's PATCH/DELETE against Org A's known player ID both return 404 (not a leak, not a silent no-op) while Org A's own PATCH/DELETE succeed, confirmed `/api/auth/me` and `/api/players` both 401 after logout and with no cookie, confirmed duplicate org slug and wrong password are rejected with the right status codes

## Step 3 — Tournament + Pod CRUD ✅
- [x] Tournament CRUD (`server/src/routes/tournaments.ts`): create/get/list/update/delete, org-scoped via `updateMany`/`deleteMany` with `{id, orgId}`, same pattern as Player
- [x] `TournamentPlayer` attach/detach (`POST`/`DELETE /api/tournaments/:id/players`), upsert-based so attaching twice is a no-op not an error
- [x] Pod CRUD (`server/src/routes/pods.ts`) nested under tournament — org-scoping here is one hop deeper (`pod.tournament.orgId`), confirmed Prisma's relation filters work in `updateMany`/`deleteMany` `where` clauses, not just `findMany`
- [x] Found and fixed a real bug before it shipped: the first draft of the pod-update schema used `podCreateSchema.partial()`, but Zod re-applies `.default()` values whenever a field is `undefined` — which is exactly what an omitted PATCH field looks like. That would've silently reset `pointsWin`/`pointsDraw`/`pointsLoss`/`roundCount`/etc. back to their defaults on every partial update that didn't mention them. Fixed by writing `podUpdateSchema` from scratch with plain `.optional()` (no defaults) on every field. Caught by reasoning about it before testing, then confirmed with a live PATCH that changes only `status` and checked the other fields survived untouched.
- [x] `Entrant` creation for both individual pods (`{playerId}`) and team pods (`{teamName, playerIds[]}`, creates Team + TeamMembers + Entrant in one transaction)
- [x] Double-booking guard: a player can't become an entrant twice in the same pod, whether individually or via two different teams — checked via `getPlayerIdsAlreadyInPod()` before creating, 409 on conflict
- [x] `DELETE /api/entrants/:id`: team entrants delete the Team (cascades to Entrant + TeamMember rows), individual entrants delete directly
- [x] Verify: recreated a real 4-team, 3-round 2-headed-giant team pod (from real tournament history, names pseudonymized here — see README's "History import" section) purely through the live API. Confirmed the returned structure matches exactly, confirmed double-booking a player into a second team 409s, confirmed the PATCH-defaults fix holds under a real request, confirmed deleting a team entrant cascades cleanly, and confirmed Org B gets 404 (not data, not a silent no-op) trying to read/delete Org A's tournament or pod by ID.

## Step 4 — Pairing engine ✅
- [x] `server/src/services/podStats.ts`: per-pod match points, opponent history, and bye history computed live from `Round`/`Match` rows (no separate stored-state table, per PLAN.md's design decision)
- [x] `server/src/services/weekendHistory.ts`: cross-pod, player-level "who's played whom this weekend" counts, resolving team entrants down to their member players so 2HG-style matches count every member of side A against every member of side B
- [x] `server/src/services/pairing.ts`: score-group sort (shuffled on round 1 for a genuine random draw, points-desc thereafter) + greedy pair-with-backtracking. Hard-avoid (within-pod repeat) is `Infinity` cost, so it's a true constraint; soft-avoid (repeat anywhere else this weekend) is a large-but-finite weight, so it degrades gracefully instead of failing when full avoidance becomes mathematically impossible
- [x] Bye handling: lowest score, no prior bye this pod, falls back to lowest score regardless if everyone's already had one
- [x] `server/src/routes/rounds.ts`: full round lifecycle — auto pairing (`POST /api/pods/:id/rounds`), manual pairing (`POST /api/pods/:id/rounds/manual`, validated so every active entrant appears exactly once and the bye count matches parity — no silent partial pairings), swap two entrant slots between pending matches (`POST /api/rounds/:id/swap`, rejects swapping a bye slot), start (locks further pairing edits, computes `endsAt` from `Pod.roundLengthMinutes`), result entry (`PATCH /api/matches/:id/result`, only while the round is ACTIVE), complete (blocked until every non-bye match has a result). Round-sequencing is enforced (`previous_round_not_completed`, `round_count_exceeded`) so pairing for round N always sees complete data for rounds 1..N-1
- [x] `GET /api/tournaments/:id/coverage`: weekend coverage matrix (player × player times-played), same underlying data the pairing soft-avoid uses
- [x] Found and fixed a real gap during live testing: `Entrant` creation didn't attach the player to `TournamentPlayer`, so someone actively playing in a pod could be invisible in the coverage view (and would have been in Gesamtwertung later) unless the organizer separately called the roster-attach endpoint. Fixed with `syncTournamentAttendance()` — upserted inside the same transaction as entrant/team creation, both branches (individual and team)
- [x] Verify — deterministic Vitest suite (`server/src/services/pairing.test.ts`) against a real throwaway Postgres, not mocks: (1) a full 8-entrant, 3-round pod never repeats an opponent, confirmed across 9 consecutive runs despite round-1's randomized draw; (2) bye assignment goes to a different entrant each round until everyone's had one; (3) given a completed Pod A where two pairs have already met, Pod B's fresh round-1 pairing for the same 4 players always avoids those pairs when an equally-valid alternative exists — this one is provably deterministic, not just probably-usually-true, because the crafted 4-player scenario has an all-zero-cost perfect matching that greedy-lowest-cost-first always finds before ever considering the higher-cost repeat option
- [x] Verify — full HTTP round-lifecycle exercised live against a running stack (not just the pure algorithm): 6-player pod through rounds 1-2 auto-paired with a verified zero repeat opponents between them, swap tested before lock and confirmed blocked after `start`, `complete` blocked until all results are in then succeeds once they are, manual pairing's validation confirmed rejecting both an incomplete roster and a wrong bye count, and cross-org 404s confirmed on round start / match result / coverage (three hops from org: round → pod → tournament → org)

## Step 5 — Match results + pod standings ✅
- [x] Result entry (organizer-only, per PLAN.md's v1 scope) — already built in Step 4 as necessary plumbing (`PATCH /api/matches/:id/result`). Nothing left to do here beyond a frontend for it in Step 9.
- [x] Extended `podStats.ts`'s core `tallyMatches()` with `matchesPlayed`/`gamesWon`/`gamesPlayed` tracking (byes count as a played round + a clean 2-0; a real match still `PENDING` correctly contributes to nothing yet, which matters for live mid-round standings even though it's a no-op for pairing since round-sequencing guarantees prior rounds are always complete by the time pairing looks at them) — added `computeAllPodStats()` alongside the existing round-cutoff `computePodStats()` rather than duplicating the tally logic
- [x] `server/src/services/standings.ts`: `computePodStandings()` — match-win% and game-win% per entrant, then OMW%/OGW% as the average of each opponent's own (floored-at-33%) percentage, sorted points → OMW% → GW% → OGW% per MTR tiebreaker order. Pulls the full entrant list from the DB (not just entrants who've played something yet) so a fresh pod shows everyone at zero rather than an empty table
- [x] `GET /api/pods/:id/standings` — full table with player/team names joined in
- [x] Verify — before writing any code, hand-reconstructed the actual round-by-round results of a real team pod from real tournament history (Rounds 1-2 were stated directly in the source doc; Round 3 wasn't recorded there, but is uniquely determined by the stated final standings — both Round 3 matches were draws, confirmed by hand-solving the points backward, then cross-checked all four rows' OMW%/GW%/OGW% by hand against the planned formula before implementing). Then wrote it as a deterministic Vitest test (`standings.test.ts`, names pseudonymized — see README's "History import" section for where the real data lives) that recreates the exact pod/teams/rounds/results and asserts the computed output against the real numbers — **all four rows match exactly**: 5/40.74/100/44.44, 4/44.44/50/61.11, 4/44.44/50/61.11, 2/48.15/0/66.67. Also verified live over HTTP: a fresh pod shows all entrants at zero, standings update correctly as results come in (and correctly exclude a match's contribution until its result is actually submitted, even mid-round), and cross-org access to another org's standings 404s.

## Step 6 — Weekend Gesamtwertung ✅
- [x] `server/src/services/gesamtwertung.ts`: `computeGesamtwertung(tournamentId)` sums each attended pod's `computePodStandings()` points per player (team pods → full team points per member, reusing the same standings computation Step 5 already built and verified — no separate point-tallying logic to drift out of sync), counts `eventsPlayed`, and ranks by average desc → total desc → name asc
- [x] `GET /api/tournaments/:id/gesamtwertung` — joins in player display names
- [x] Before writing the aggregation test, hand-verified (via a throwaway Node script, not by eye) exactly what a real re-rank looks like in real tournament history (details pseudonymized here, real data outside this repo — see README's "History import" section): one player (27 pts over 5 pods actually attended, avg 5.4) jumps from rank 6 by raw total up to **rank 4** by average, passing two others with higher totals but more pods played (31/6 = 5.17 and 30/6 = 5.0) — confirms the plan's "average, not total" rule produces a real, meaningful re-rank on real data, not just a theoretical one
- [x] Verify — rather than refitting all 10 real Bad Gechingen players (which would require inventing an internally-consistent 60-cell match history just to hit specific point totals, disproportionate effort given Step 5's standings math is already separately verified against real data), wrote a small fully-hand-checked synthetic scenario in `gesamtwertung.test.ts` that isolates the same property precisely: two players tie on raw total (6 pts each) but one earned it over fewer pods, and must rank above the other by average — passes, plus a second test confirming team-pod full-point-crediting holds through the tournament-wide aggregation too. Also checked live over HTTP with cross-org 404 confirmed.

## Step 7 — Realtime + round timer (backend half) ✅
Per the decision to stay backend-first through Step 8, this step is scoped down to what's actually backend-buildable. The frontend-dependent pieces — TanStack Query cache invalidation, the Display Mode audible chime, and the two-browser-window visual verification — move to **Step 9** alongside the rest of the UI; listed there now so they aren't lost.
- [x] `server/src/realtime.ts`: Socket.IO server, rooms are `pod:<id>` / `tournament:<id>`. Sockets are read-only subscriptions — joining a room needs no auth (same trust model as the public HTTP pages: an unguessable id is the access control), all actual mutations still go through the authenticated HTTP API, which is the only thing that ever triggers a broadcast
- [x] Wired into `index.ts` via Fastify's `serverFactory` + a raw `http.Server`, with the ordering called out explicitly in a comment: Socket.IO's `attach()` saves and wraps whatever `request` listeners already exist, so Fastify's listener (registered via `serverFactory`, which runs synchronously inside the `Fastify(...)` call) has to be in place *before* `initRealtime()` creates the Socket.IO server, not after. Got this backwards on the first pass while reasoning through it, caught it before writing any code by working through what `attach()` actually does, not just copying a snippet
- [x] Broadcasts wired into `rounds.ts`: `pairings-published` (auto or manual round creation), `pairings-updated` (swap), `round-started`, `round-extended` (new `POST /api/rounds/:id/extend` endpoint — the "+5 min" nudge), `round-completed`, `result-submitted`. Card-pull broadcasts wait for Step 8, which hasn't built card pulls yet
- [x] Verify — didn't just trust the reasoning above, actually tested the tricky part: confirmed plain HTTP routes (`/api/healthz`, the SPA, a 404 for an unknown API path) still work AND the Socket.IO engine.io handshake (`/socket.io/?EIO=4&transport=polling`) responds correctly on the same shared server. Then a real Socket.IO client (`socket.io-client`, run standalone against the live stack) joined a pod room and received all 5 broadcast events in the correct order with correct payloads while the round lifecycle was driven over HTTP — including `round-extended` showing exactly `startedAt + 5min` on `endsAt`. Finally confirmed room isolation two ways: a client that joined a *different* pod's room, and a client that joined no room at all, both received zero events while a real broadcast was firing elsewhere.

## Step 8 — Value tracking (backend half) ✅
Backend-first, same as Step 7 — the add-pull UI and the Hall of Fame *page* need real UI and move to Step 9. Everything they'd call is built and verified here.
- [x] `server/src/services/scryfall.ts`: proxies `/cards/autocomplete` + `/cards/named` with a proper descriptive User-Agent (Scryfall's API guidelines require identifying yourself) and a short in-memory TTL cache (5 min autocomplete, 1 hour named-card lookups) — a full rate limiter would be over-engineering at this app's actual traffic scale (a handful of organizers adding a few pulls per pod), the cache is the practical mitigation
- [x] `POST /api/pods/:id/card-pulls`: takes a card name, resolves it live via Scryfall, snapshots `scryfallId`/`setCode`/`priceEur`/`imageUri` into `CardPull` at add-time (deliberately a snapshot, not a live reference — per PLAN.md, prices fluctuate and history should stay historical)
- [x] `GET /api/pods/:id/card-pulls`: per-pod gallery sorted by price desc, with a running total
- [x] `GET /api/tournaments/:id/card-pulls`: "Best Pulls of the Weekend" rollup across every pod in a tournament
- [x] `GET /api/card-pulls/hall-of-fame`: all-time, across every tournament the org has ever run, top 25 by price
- [x] `DELETE /api/card-pulls/:id`
- [x] Verify — confirmed real outbound network access from a container first (not assumed), then re-added the actual 5 historical 2025 Sommer Battlebond pulls (Morphic Pool, Doubling Season, Spellseeker, Diabolic Intent, Seedborn Muse) through the live API against **today's real Scryfall data**, not mocked responses. Prices came back close to but not identical to the 2025 historical figures (e.g. Morphic Pool €25.98 today vs €25.91 then) — expected and correct, since these are live current prices and the whole point of snapshotting at add-time is that history stays historical while new pulls reflect the market as it is now; matching the exact 2025 total would have meant the integration was somehow returning stale data. Confirmed the gallery total sums exactly (€81.70 for the 5 cards' current prices), confirmed autocomplete and a real card-not-found case both behave correctly, confirmed delete updates the total correctly, and confirmed cross-org isolation on all five endpoints including Hall of Fame correctly returning empty rather than leaking another org's pulls.

## Step 9 — Public pages + polish (the real frontend build) ✅
This is the biggest single step in the plan — it's being worked in phases, each checked in on rather than delivered silently as one giant commit. Design direction was reviewed and approved first, via a standalone HTML mockup built with real 2025 Sommer data (see `PLAN.md`) and validated against the dataviz skill's accessibility checker — **dark mode only for v1** (light mode is a deliberate, explicitly-requested v2, not an oversight — the token structure supports adding it later without a rewrite).

### Phase A — infrastructure, auth, first real page ✅
- [x] `react-router-dom` added; client restructured into `routes/`, `features/<resource>/`, `components/`, `lib/`
- [x] Design system ported from the approved mockup into Tailwind v4 `@theme` tokens (`client/src/index.css`) — same validated accent (`#a67c27`, re-picked after the dataviz palette validator flagged the first choice as too close to "warning"), same Georgia-for-display/system-sans-for-data type pairing
- [x] `lib/api.ts` (typed fetch wrapper, cookie-based auth via `credentials: "include"`) and `lib/types.ts` (shared domain types matching backend response shapes)
- [x] Auth: `useAuth.ts` (TanStack Query — `useMe`/`useSignup`/`useLogin`/`useLogout`), `LoginPage`, `SignupPage` (with live slug auto-derivation from org name), `ProtectedRoute` (redirects to `/login` when `useMe` resolves to `null`)
- [x] `Layout` (topbar: wordmark, org context, log out) + `DashboardPage` (real tournament list + inline create-tournament form, not a stub) + minimal `TournamentPage` (tournament header, pod count) — first real end-to-end data page
- [x] Verify — no browser automation tool was connected in this environment (`claude-in-chrome` unavailable, `chromium-cli` not installed), so followed the `run` skill's documented fallback: Playwright directly via the official `mcr.microsoft.com/playwright` Docker image against the live compose stack. Drove the actual app end-to-end — signup → dashboard → create tournament → tournament detail page → reload (session persists) → logout → confirm the protected route redirects again — and **looked at the resulting screenshots**, not just the exit code, to confirm the design actually renders correctly (dark theme, Georgia headings, gold accent, real data flowing through). The only console entries were expected 401s from the auth check while intentionally logged out, not runtime errors.

### Phase B — roster, pod setup, entrant/team assignment ✅
- [x] `RosterPage` (`/roster`): list + rename + remove + add, using the Step 2 `/api/players` endpoints. Added to the topbar nav alongside Tournaments.
- [x] `TournamentPage` expanded: pod list (links to `/pods/:id`) + a "New pod" form covering the full `Pod` config — name, format, date, team-event toggle (reveals team size), and the Swiss parameters (round count, match format, points win/draw/loss, round length) tucked behind a `<details>` "Advanced settings" disclosure so the common case stays simple
- [x] `PodPage` (`/pods/:id`): branches on `isTeamEvent`. Individual pods get a player-select + add/remove list; team pods get a team-name + multi-checkbox form + add/remove, with the "everyone's already entered" state correctly hiding the form once the roster is consumed (exercising the Step 3 double-booking guard from the actual UI)
- [x] **Found and fixed a real bug via browser testing, not caught by typecheck or the API tests**: after login, `useLogin`'s `onSuccess` called `queryClient.invalidateQueries(["me"])` rather than setting the cache directly. An invalidated query still serves its previous (unauthenticated, `null`) data until the background refetch resolves — and `ProtectedRoute` reads exactly that stale `null` in the gap, redirecting straight back to `/login` before the real session data ever arrives. The topbar would show a logged-in nav while the page underneath was still the login form — a state that only a real click-through in a real browser would surface, not a type error or an API-level test. Fixed by having `POST /api/auth/login` return the full `{organizer, organization}` shape (matching what signup already returns) and setting the query cache synchronously with it, the same pattern `useSignup` already used successfully.
- [x] Verify — no browser tool connected in this environment, so continued with the Playwright-via-Docker-image fallback from Phase A. Recreated the actual 2025 Sommer Battlebond lineup (4 real teams from 9 real roster names) entirely through the UI and confirmed it visually; separately verified the individual-entrant path (not exercised by the team-pod flow) with add/remove; the login race above was caught specifically because the driver script asserted on the resulting URL rather than just the API response, which is exactly the class of bug that verification step exists to catch.

### Remaining phases (not started)
- [ ] Phase C — the flagship pages: Gesamtwertung (hero ranking list), pod standings (data table), live pairings view. Also where the rest of Step 7's frontend half lands, since sockets matter most here:
  - [x] Gesamtwertung page (`/tournaments/:id/gesamtwertung`) — hero ranking list matching the approved mockup: rank badge (gold for #1), average as the hero number, total secondary, per-pod pip row. Extended the backend's `computeGesamtwertung()` to return a `perPod` breakdown first (`GET .../gesamtwertung` now returns `{pods, gesamtwertung}`), keeping "never attended" (absent from the map) distinct from "attended and scored zero" — verified live with a seeded 2-pod/4-player scenario where one player skipped a pod: the skipped pod correctly renders as `–`, not `0`, and the average-tie tiebreak-by-total (two players tied at 3.0 avg, separated by total) renders in the right order
  - [x] Pod standings page (`/pods/:id/standings`) — data table matching the mockup, leader row gets the gold left-border stripe, tabular figures throughout. Extracted `PodTabs` (Entrants/Pairings/Standings) as a shared component since three pod-scoped pages now need it, and `entrantDisplayName` to `lib/entrant.ts` since it was about to be defined a third time.
  - [x] Pairings page (`/pods/:id/rounds`) — the biggest single piece of frontend so far: round cards (pair/start/complete actions gated by status), match cards with a game-score entry form that derives the result client-side from the two numbers rather than requiring separate win/draw/lose buttons, bye display, read-only final scores once reported. Manual pairing and the swap-before-lock UI are **not** built yet — auto-pairing is the only path in the UI right now; the API supports manual/swap already (Step 4), just no frontend for it yet.
  - [x] Verify — the most thorough browser test run so far, not a quick smoke check: signed up, built a 6-player roster, created a pod, paired and played all 3 rounds to completion through real UI clicks (not API shortcuts) for round 1, then finished rounds 2-3 via API (to keep the run fast) and screenshotted the resulting pairings/standings pages. Confirmed from the actual rendered output — not by trusting the algorithm — that all 6 players got 3 distinct opponents across 3 rounds (a perfect round-robin), and that the final standings table's points and tiebreaker percentages match hand-checkable arithmetic (Dave went 3-0 for 9 points). Also hit and diagnosed one test-script flake (added a robustness wait) — confirmed via a direct API call that it was a Playwright timing issue, not an app bug, before moving on rather than assuming.
  - [x] TanStack Query cache invalidation on the Socket.IO events already being broadcast. `usePodRealtime(podId, tournamentId)` joins the pod room and invalidates on all six pod events; also added a `tournament:<id>` room + `standings-changed` broadcast (not in the original event list — added because Gesamtwertung reflects every pod's results, so it needs a tournament-wide signal, not a pod-scoped one) emitted from both `round-completed` and `result-submitted` in `rounds.ts`, consumed by a new `useTournamentRealtime` hook on the Gesamtwertung page.
  - [x] Verify — the actual two-browser-window test the plan called for, not simulated: one Playwright browser context, two separate pages both on the same pod's Pairings tab, sharing a login session. Page A left untouched; Page B submitted a real result through its own UI form. Page A transitioned from a `0–0` entry form to a `2–0` final score with zero interaction or reload — screenshotted both states to confirm, not just asserted on network traffic.
  - [x] Client-side round timer (`useCountdown`, `lib/useCountdown.ts`) ticking locally from `Round.endsAt` — no server chatter needed to tick, only the initial `endsAt` (arrives via the realtime `round-started`/`round-extended` broadcasts already wired above). "+5 min" button was already built with the round card; now it visibly moves the countdown.
  - [x] Display Mode toggle on `PairingsPage`: enlarges the active round's timer (26px → 64px) and is the sole gate on the audible chime (`lib/chime.ts`, a synthesized two-tone Web Audio beep — no asset file, no CSP concerns). Personal/non-toggled views show the same countdown but never play the chime, matching "stay muted by default."
  - [x] Verify — this is the one piece of Step 9 that couldn't be shortcut via API seeding, since the whole point is real elapsed time: seeded a pod with the minimum round length (1 minute — `roundLengthMinutes` floors at 1), started the round through the actual UI, and let it genuinely count down to zero rather than faking the clock. Confirmed the countdown visibly ticks (1:00 → 0:48 after waiting 12 real seconds). For the chime specifically — headless Chromium can't be "listened to" — intercepted `window.AudioContext` via a Playwright init script to detect construction, which is what `playChime()` actually does when it fires. Ran two pages against the same live round in parallel: Display Mode ON correctly triggered `AudioContext` construction at expiry, Display Mode OFF on a second page watching the identical round correctly did **not** — confirming the "muted by default" requirement isn't just a comment, it actually holds live.
- [x] Phase D — value tracking UI, the rest of Step 8's frontend half ✅
  - [x] Add-pull form (`PodValuePage`, new "Value" tab in `PodTabs`): debounced autocomplete dropdown against `GET /api/scryfall/autocomplete`, selecting a suggestion (or pressing enter) submits directly — no separate preview/confirm step, since the backend's own fuzzy Scryfall lookup on add is already reliable and a two-step flow would be extra friction for no real safety benefit at this app's scale
  - [x] `CardGallery` shared component (used by pod/tournament/Hall-of-Fame views): real Scryfall card images via `object-cover` at the card's native ~5:7 aspect ratio — the mockup used abstract gradient placeholders only because the Artifact sandbox blocks external images; the real app has no such restriction, so it shows actual card art, which turned out nicer than the mockup itself
  - [x] `TournamentValuePage` ("Best pulls of the weekend") and `HallOfFamePage` (org-wide, top 25) — both read-only rollups over the same gallery component. Added a "Hall of Fame" nav link to the topbar and a "Best pulls" link alongside Gesamtwertung on `TournamentPage`.
  - [x] Found and fixed a minor real navigation gap while building this: `PodPage` (the Entrants tab) was the only pod-scoped page with no link back to its tournament, unlike Pairings/Standings/Value which all had one — fixed to match.
  - [x] Verify — recreated the real 2025 Sommer Battlebond pulls a second time, this time through the actual autocomplete UI rather than curl (Step 8 only verified the backend directly): typed each card name, picked it from the live dropdown, confirmed the total matched the earlier API-only test exactly (€81.70), and confirmed a real card image actually loaded (naturalWidth 488px, Scryfall's real image size — not a broken `<img>`). Also checked the tournament rollup and Hall of Fame pages render the same data correctly, sorted by price.
- [x] Phase E — public unauthenticated `/o/<slug>/tournaments/<id>` + `.../pods/<podId>` pages, plus the final responsive pass (usable on a phone checking standings and on a shared iPad/TV in Display Mode) ✅
  - [x] Backend: `server/src/routes/public.ts`, a parallel read-only surface under `/api/public/o/:slug/...` scoped by org slug (`findPublicTournament`/`findPublicPod` in `ownership.ts`) instead of session orgId — no auth hook, mirrors the private endpoints' response shapes (tournament+pods+players, gesamtwertung, pod+entrants, rounds, standings, card-pulls) so the frontend can reuse the same types. Verified live with curl and no cookie at all: tournament/gesamtwertung/pod all return real data, and a wrong org slug correctly 404s rather than leaking anything.
  - [x] Frontend: `features/public/usePublic.ts` (all seven public data hooks, `["public", ...]`-prefixed query keys so they never collide with the authenticated cache namespace), `PublicLayout` (topbar wordmark only, no auth chrome, wraps `<Outlet/>`), `PublicTournamentPage` (pod list + Gesamtwertung — extracted the ranking list into a shared `GesamtwertungList` component so the private `GesamtwertungPage` and this one don't duplicate the rank-badge/tie-handling/pip-row logic a third time) and `PublicPodPage` (Pairings/Standings/Value as three sections on one scrolling page, deliberately not tabbed — matches the plan's spec and the original review mockup's section-scrolling pattern; pairings are read-only, no result-entry forms, timer still counts down live). Registered as a sibling top-level route block in `main.tsx` (`/o/:slug/tournaments/:id` and `.../pods/:podId`) ahead of the authenticated `Layout` block. Added "Public link ↗" out-links on `TournamentPage` and `PodPage` (opens in a new tab, built from `useMe().organization.slug`).
  - [x] Realtime had to change to serve both namespaces off the same events: `usePodRealtime`/`useTournamentRealtime` switched from fixed-prefix `queryKey` invalidation to `predicate: (query) => query.queryKey.includes(id)`, so a `["public", "pod", id, ...]` key and an authenticated `["pod", id, ...]` key both react to the same Socket.IO broadcast without two parallel invalidation paths.
  - [x] Verify — two separate Playwright browser **contexts** in the same run, not two tabs sharing cookies: Context 1 logged in as the organizer, opened the real `TournamentPage`, and scraped the actual rendered "Public link" `href` out of the DOM (not a hand-typed URL guess). Context 2 was a brand-new context with zero cookies ever set, navigated straight to that scraped URL. Confirmed the tournament page loads with real seeded data (Bob/Alice/Carol/Dave Gesamtwertung, correctly ranked), a DOM query for auth-only chrome (`Log out`, `TOURNAMENTS`, `ROSTER`) returned 0 matches, and the pod page shows all three sections live — the actual submitted `2–1` result under Pairings, the standings table, and the "Doubling Season" pull under Value — with zero console errors. Screenshots inspected directly (not just asserted): both pages render correctly against the approved dark trophy-plaque design system, gold accent, tabular figures throughout.
  - [x] Final responsive pass — measured, not guessed: seeded a real 6-player pod (Alice–Frank) with a completed round (real `2–1` results, not `0–0` placeholders) and a card pull, then drove every authenticated route plus both public routes through Playwright at a 390×844 phone viewport, checking `document.documentElement.scrollWidth > clientWidth` after each navigation rather than eyeballing for overflow. Found genuine horizontal overflow on **every single authenticated page** — the public pages were already clean. Root cause: `Layout`'s topbar (`components/Layout.tsx`) laid out logo, org-name pill, nav links, and log-out all in one non-wrapping flex row, needing ~800px minimum. Fixed by making the topbar `flex-wrap`: row 1 is logo + log-out (`ml-auto` keeps them at opposite ends even wrapped), the org-name pill hides below `sm:` (it's redundant with page content), and nav drops to `order-3 w-full` — its own row, horizontally scrollable as a fallback rather than ever forcing page-level overflow. Applied the same `px-4 sm:px-8` padding trim to `PublicLayout` for consistency (it wasn't broken, just more cramped than necessary at phone width). Also switched the three hard `grid-cols-2` form layouts (new-tournament dates, new-pod name/format, new-pod advanced settings) to `grid-cols-1 sm:grid-cols-2` so form fields aren't squeezed to ~150px on a phone. Re-ran the same measured check after the fix: zero pages overflow. Also screenshotted the public pod page at 1024×768 (iPad landscape, the actual Display Mode hardware target) — pairings/standings/value all render at a comfortable, glanceable size with no layout changes needed, confirming the existing design already scaled correctly for the shared-screen case and only the phone/topbar case needed work.

## Step 10 — History import ✅
- [x] Went back to the actual Outline "GP Eichstätt" collection via MCP rather than transcribing only from PLAN.md's summary — this caught three real things PLAN.md's summary didn't capture: (1) a "War of The Sparks" doc exists in 2024's collection but is empty and isn't linked from that tournament's events list — confirmed empty, correctly excluded, matching PLAN.md's 6-pod table; (2) no document anywhere (top-level tournament doc or Gesamtwertung doc) records an exact calendar date, only season names; (3) each tournament's top-level "Teilnehmer" list is unreliable — 2025 Sommer's and 2026 Sommer's are byte-for-byte identical (evidently copy-pasted template text, never updated), so the real per-tournament roster was taken from each Gesamtwertung table's actual column headers instead, which is the ground truth for who really played.
- [x] Name-drift across years resolved to one canonical roster (12 players) — directly confirmed drift, same spelling variants for the same person visible even *within* a single doc (one year's rank table spells a name differently than its own main table just below it). One inference, documented in `legacy-data.local.json`'s `_comment` rather than silently assumed: one year's Gesamtwertung table used two players' surnames where every other document used their first-name-plus-initial — merged on the strength of the initials matching exactly and no other document ever mentioning those surnames. (Full detail, including the actual names involved, lives in `legacy-data.local.json` itself — gitignored, not in this repo, see README's "History import" section.)
- [x] Exact dates aren't recorded anywhere in the source — rather than fabricate false precision, the data marks each tournament's dates `"dateApproximate": true` and derives them from the best available signal (MTG set release timing for 2024/2025; the current session date for 2026, whose Fri–Sun this literally is). Easy to correct later if wrong; nothing else in the app depends on the exact day.
- [x] **Schema gap found and fixed**: the plan called for importing pods that only have a final-standings table (no round-by-round data) "with final points... but no synthetic match-by-match data — no fabricating pairings that were never recorded." The schema as actually built in Step 3 had no way to represent that — `computePodStandings` always derived points from real `Match` rows, so there was no path to show a correct point total without inventing fake matches. Fixed with a small migration: `Entrant.finalPointsOverride Int?` — when set, `computePodStandings` (`server/src/services/standings.ts`) reports it directly and zeroes the tiebreaker columns (OMW%/GW%/OGW% have nothing to compute from, matching what the original docs actually showed: points-only rankings). Covered by a new unit test in `standings.test.ts`. Full test suite re-run after the migration: 7/7 pass.
- [x] `legacy-data.local.json` (gitignored, kept outside the repo — see the 2026-08-24 privacy correction note above) — all 4 tournaments transcribed from the live Outline docs (not just PLAN.md), including the 2025 Sommer/Winter Value tables PLAN.md's original summary hadn't captured (38 real card pulls total, not just the 5 Battlebond ones originally quoted). Every pod's per-player point column re-summed by hand against the doc's own "Gesamt" total before writing this file — all 39 player-tournament totals matched exactly, zero transcription errors found.
- [x] `server/src/scripts/import-legacy.ts` — Prisma-client script (bypasses the HTTP API entirely, matching the plan). Idempotent: re-running it skips anything that already exists (org by slug, tournament by name, pod by name) rather than duplicating. Creates the org + one organizer login, reading `IMPORT_ORGANIZER_EMAIL`/`_PASSWORD`/`_NAME` env vars with a random-password fallback (printed once, loudly, if not set). Standings-only pods get `Entrant.finalPointsOverride`; Battlebond (the one pod with real round-by-round data, already reconstructed in `standings.test.ts`) gets actual `Team`/`Round`/`Match` rows instead. Card pulls get a live Scryfall lookup for image/set/id (same as a real add-pull) while keeping the doc's historical EUR price rather than Scryfall's current price.
- [x] **Found and fixed a real bug during verification, not assumed away**: the first full run left 18 of 38 card pulls with no image — traced with a direct `curl` loop (not guessed) to genuine Scryfall `429`s from firing ~40 lookups back-to-back with only a short delay. Standalone retries of the same exact query always succeeded, confirming it wasn't bad data. Fixed with a real cooldown (500ms between cards, 3 attempts with growing 3s/6s backoff on failure) rather than a token retry. Re-ran against a fully wiped dev database: 38/38 card pulls resolved with a real `imageUri`, zero warnings.
- [x] Verify — ran the script against a clean `docker compose` dev stack, then checked the real output, not just exit codes: `GET .../tournaments/:id/gesamtwertung` for the 2024 tournament returned the exact average-ranked order hand-verified earlier (10 players, decimal averages from 6.0 down to 1.667, with the documented rank-4 re-rank landing exactly where predicted), matching to the decimal. Screenshotted the actual rendered pages: 2024 Gesamtwertung (correct gold rank-1 badge, correct `–` pips for the two players who skipped one pod), a real team pod's standings (5/40.74/100/44.44 down to 2/48.15/0/66.67 — exact match to the hand-verified fixture), 2025 Sommer's Gesamtwertung (a real tie at rank 5, both players showing 17/3.4), the Hall of Fame page (25 cards sorted by price, real Scryfall art throughout, top pull at €32.61), and the 2026 tournament (6 empty `SETUP` pods, 8-player roster, zero fabricated data) — all matched the source docs exactly. (Player names withheld here — see README's "History import" section.)

## Step 11 — README, LICENSE, packaging polish ✅
- [x] `README.md` final pass: rewritten status section (feature-complete, not "under construction"), a real screenshot (`docs/screenshots/gesamtwertung.png`, an average-rank Gesamtwertung page — real UI, seeded data, not a mockup; regenerated 2026-08-24 with fake players after the original used real imported data), and a new "History import" section documenting the `import-legacy` script + its env vars. First written 2026-08-23, before Step 9/10 existed; this pass brings it back in sync with what's actually built.
- [x] `LICENSE` — MIT, per the plan's stated default (never explicitly re-confirmed with Tobias before this push since "keep going" was the active instruction; flagged in the session's final summary as easy to swap to AGPL if he'd rather guard against closed-source SaaS forks — one-file change, no code depends on the choice).
- [x] Final `.env.example` pass — already complete (Postgres creds, `PORT`, `SESSION_SECRET`, `SESSION_COOKIE_SECURE`, local-dev `DATABASE_URL`); the import script's own env vars (`IMPORT_ORG_SLUG` etc.) are one-time-use, documented in the README's History import section instead of cluttering the file everyone edits to just run the app.
- [x] **Resolved 2026-08-24** (was flagged here for Tobias, not decided unilaterally — see the correction note at the top of this file): real friend-group data doesn't belong in the repo. `import-legacy.ts` now reads a runtime-supplied file instead of compiling one in; the real data moved to a gitignored `legacy-data.local.json`; every real name/result was scrubbed from `PLAN.md`, `STEPS.md`, both test files, and the screenshot; git history was rewritten and force-pushed to remove it from past commits too.

## Step 12 — Deploy to DaxLite
- [x] Deployment guide written: `docs/deploy-daxlite.md` (clone-to-DaxLite steps, `.env` values, a production note about dropping the dev-only Postgres port publish, the LAN-vs-Tailscale exposure decision, first-boot command, the optional history-import step). **Removed from the repo 2026-08-25** ahead of the public GitHub release — it was personal runbook content (real Tailscale/Forgejo hostname, private `claude-memory` cross-references), which already ran against this same file's own stated scope ("this repo's docs stay scoped to the project itself"). The general guide (`docs/deployment.md`) covers the same ground without any of that; DaxLite-specific notes now live only in the private `claude-memory` repo, per that repo's own convention.
- [ ] **Tobias runs this** — touches live infra outside this session's reach. Not attempted here.
- [ ] Once live: add a `services/daxlite.md` entry in `claude-memory`

## Post-1.0 improvements (backlog)

Feature work requested after the initial build shipped and the history import went live. Unlike Steps 0-12 these are **not** a strict sequence — pick them up independently. Written detailed enough to implement cold in a future session. (Separate, already-shipped post-1.0 work not re-listed here: `Match.gamesDrawn` for MTR-correct game-win% on tied games, and the full round-by-round history backfill from Outline — both committed, see git log `dba4830`/`a0a8bad`.)

### PI-1 — Public tournament page: "Card values" overview ✅ (implemented 2026-08-24, not yet deployed)
- [x] The public `/o/<slug>/tournaments/<id>` page showed only the pod list + Gesamtwertung; added a "Card values" section so a shared link is a one-glance overview of the weekend's opened cards, by event and price.
- [x] Frontend-only — the backend endpoint already existed (`GET /api/public/o/:slug/tournaments/:id/card-pulls`, price-sorted, each pull carries its `pod`). `PublicTournamentPage.tsx`: new `usePublicTournamentCardPulls` query; pulls grouped by pod, groups ordered by the tournament's own pod sequence ("by event, highest value first"); each group is a `CardGallery` with a per-pod subtotal, plus a weekend total. Reuses `CardGallery` / `formatEur`.
- [ ] Still to do: commit, deploy to daxlite, and eyeball it on a real shared link.

### PI-2 — Rename the "Hall of Fame" nav item (cards) → "Treasure Chest" ✅ (implemented 2026-08-24, not yet deployed)
- [x] The org-wide most-valuable-cards gallery is renamed to **Treasure Chest**, freeing the "Hall of Fame" name for PI-3's player aggregate.
- [x] Frontend: `HallOfFamePage.tsx` → `TreasureChestPage.tsx` (component + title/dek); route `/hall-of-fame` → `/treasure-chest` in `main.tsx`; nav label in `components/Layout.tsx`.
- [x] Backend renamed too (did the full rename, not the optional skip): `GET /api/card-pulls/hall-of-fame` → `/api/card-pulls/treasure-chest` (`server/src/routes/cardPulls.ts`) + the `useHallOfFame` hook → `useTreasureChest` (query key included). No data change.
- [x] Deployed & verified 2026-08-24 (Tobias confirmed the rename + both pages).

### PI-3 — New "Hall of Fame" page: all-time player aggregate ✅ (implemented 2026-08-24, not yet deployed)
- [x] All-time leaderboard aggregated across **every** tournament the org has run, in the "Hall of Fame" nav slot PI-2 freed up.
- [x] Backend: `server/src/services/hallOfFame.ts` `computeHallOfFame(orgId)` (mirrors `gesamtwertung.ts`) — walks every pod in the org, reuses `computePodStandings(pod.id)` for each entrant's points, resolves team entrants to members (**full** credit, same as Gesamtwertung), aggregates per player: tournaments played, pods played, total points, all-time average pts/pod; ranks by avg → total → name. Skips empty/unplayed pods.
- [x] Endpoint: `GET /api/hall-of-fame` (authed, org-scoped) in `server/src/routes/hallOfFame.ts`, registered in `index.ts`.
- [x] Frontend: `client/src/routes/HallOfFamePage.tsx` at `/hall-of-fame` + `useHallOfFame` hook; ranked table reusing the Gesamtwertung rank-badge styling (`rankBadgeClasses` exported from `GesamtwertungList.tsx`): rank, player, pods, tournaments, avg pts/pod, total. Nav link added back in `Layout.tsx`.
- [x] Decisions taken: columns = rank/player/pods/tournaments/avg/total (no "tournament wins" — that richer set lands in PI-7); team-pod points count full (consistent with Gesamtwertung); board is **authed-only for now** (no public mirror built — revisit if a public all-time board is wanted).
- [x] Deployed & verified 2026-08-24 (Tobias confirmed the all-time leaderboard looks right against the imported history).
- [ ] Nice-to-have: a unit test mirroring `standings.test.ts` against a throwaway Postgres for coverage — not added yet.
- [ ] PI-6's exclude-from-stats filter has a marked spot in `computeHallOfFame` (comment) for when that flag exists.

### PI-4 — Built-in MCP server for agent-driven editing ✅ (implemented 2026-08-24, not yet deployed)
- [x] Goal met: `mcp/` is a thin façade over the existing HTTP API (not the DB) — pairing/standings/validation/multi-tenancy stay authoritative on the server, matching the plan's stated approach.
- [x] **Auth — bearer token scoped to an organizer, as decided.** `ApiToken` model (`organizerId` FK, SHA-256 `tokenHash`, `name`, `createdAt`, `lastUsedAt`) added via migration `20260824153858_add_api_token`. `requireAuth` (`server/src/auth/middleware.ts`) now accepts `Authorization: Bearer <token>` as a fallback when there's no session cookie — looks the token up by hash, loads its organizer, sets `request.organizer` exactly as a session would, and best-effort bumps `lastUsedAt`. `POST`/`GET`/`DELETE /api/api-tokens` mint/list/revoke. **Extra hardening beyond the plan's own spec**, caught while writing the mint route's own doc comment: the token-management routes must NOT accept bearer auth themselves (otherwise a leaked token could mint/revoke tokens, including replacing itself after "revocation"). Added `requireSessionAuth` (session-cookie only, no bearer fallback) and gated `apiTokens.ts` on it instead of the regular `requireAuth`. Minimal frontend: `ApiTokensPage` (mint shows plaintext once, list + revoke) plus an "API Tokens" topbar link, so a token can be minted without curl.
- [x] **Full read/write, runs locally, as decided.** New `mcp/` npm workspace (added to root `workspaces`), `@modelcontextprotocol/sdk@1.30.0` over stdio, `LIMITED_GAUNTLET_URL`/`LIMITED_GAUNTLET_TOKEN` env vars (fails fast at import time if missing, matching the app's own `SESSION_SECRET` convention — see `mcp/README.md`). 32 tools total:
  - reads: tournaments, pods, standings, rounds, gesamtwertung, coverage, pod/tournament/org-wide card pulls, hall-of-fame overview + per-player detail, roster
  - non-destructive writes: create/update tournament, create/update pod, add individual/team entrant, generate/manually pair a round, swap pairing, start/extend/complete round, submit match result, add card pull — execute directly
  - destructive (confirm-gated): `delete_tournament`, `delete_pod`, `remove_entrant`, `delete_card_pull`
- [x] **Destructive writes require confirmation, as decided.** `registerDestructiveTool` in `mcp/src/index.ts`: first call runs a dry-run `describeImpact` (e.g. "Would delete tournament X and its N pod(s): ...") and returns a single-use, 5-minute confirmation token (`mcp/src/confirm.ts`, in-process `Map`); a second call with that token executes; `confirm: true` on the first call skips straight to execution. Verified the token is genuinely single-use (a replay attempt is rejected, not silently re-executed) and expires.
- [x] **Real bug found and fixed via live end-to-end testing, not just typechecking**: every `DELETE` call from `mcp/src/client.ts` sent `Content-Type: application/json` with no body, which Fastify's JSON body parser rejects (`FST_ERR_CTP_EMPTY_JSON_BODY`) — every destructive tool would have 400'd on its real execute step despite the dry-run working fine. Fixed by only setting the header when a body is actually present, matching the guard `client/src/lib/api.ts` already used for the exact same reason.
- [x] Verify — spawned the actual compiled server as a subprocess over the real MCP stdio protocol (`@modelcontextprotocol/sdk`'s `Client`/`StdioClientTransport`, not a mocked harness) against the live compose stack: confirmed all 32 tools list correctly; ran a full `list_players → create_tournament → create_pod → add_individual_entrant ×2 → generate_round → start_round → submit_match_result → complete_round → get_pod_standings` flow entirely through MCP tools and got the correct 3/0 point split back; confirmed the delete-confirm flow end-to-end (dry run → confirm → tournament actually gone, verified with a follow-up `get_tournament` 404) and confirmed a reused confirmation token is rejected. Full server test suite (7/7) and both `tsc -b` builds still green after the schema/middleware changes.
- [ ] Not yet deployed — `mcp/` needs `npm install`/`npm run build --workspace mcp` wherever it's meant to run (the dev machine, per the plan, over Tailscale to daxlite), and a real token minted from the deployed app's API Tokens page.

### PI-5 — Edit / delete existing tournament data (day-of corrections + test cleanup) ✅ (implemented 2026-08-24, not yet deployed)
Motivation: right now there's no way to fix a fat-fingered score mid-day, and no way to remove a test pod — so experimenting risks polluting real data.
- [x] **Edit round scores after the fact.** `PATCH /api/matches/:id/result` (`server/src/routes/rounds.ts`) now also accepts edits while the round is `COMPLETED`, not just `ACTIVE` — `round_not_active` only fires for a still-`PENDING` round. No standings/Gesamtwertung fix-up needed — they're computed live from `Match` rows, so a corrected score propagates automatically; the existing `result-submitted` / `standings-changed` broadcasts already fire on every save. Frontend: `PairingsPage.tsx`'s `ResultEntry` gained an "Edit" link on a reported score that reopens the same entry form (pre-filled), with Cancel to back out.
- [x] **Edit + delete pods from the UI.** Backend CRUD already existed. New: `useUpdatePod`/`useDeletePod` hooks (`usePods.ts`), an `EditPodForm` on `PodPage` (name, date, format, `roundCount`, `matchFormat`, points config, `roundLengthMinutes`) behind an "Edit pod" toggle, and a "Delete pod" button (`window.confirm`, matching the existing `RosterPage` remove-player pattern) that navigates back to the tournament page on success.
- [x] **Edit tournament fields.** New: `useDeleteTournament` hook, an `EditTournamentForm` on `TournamentPage` (name, start/end dates, location, status) behind an "Edit tournament" toggle, and a "Delete tournament" button (confirm dialog, navigates to `/` on success).
- [x] Verify — live against a running `docker compose` stack, not just reasoning: signed up two orgs, built a real 4-player pod through to a completed round, confirmed editing a reported result (Alice/Bob's match, `A_WINS 2–0` → `DRAW 1–1`) actually moved both the pod standings (Alice 3→1pt, Bob 0→1pt) and the tournament's Gesamtwertung in the same request. Confirmed all five relevant endpoints (`PATCH`/`DELETE` tournament, `PATCH`/`DELETE` pod, `PATCH` match result) 404 for a second org against the first org's data, while the owning org's own edits return 200. Full server test suite (7/7, `standings`/`gesamtwertung`/`pairing`) still green.

### PI-6 — Per-pod "exclude from stats" flag ✅ (implemented 2026-08-24, not yet deployed)
- [x] `Pod.excludeFromStats Boolean @default(false)` added (migration `20260824135321_add_pod_exclude_from_stats`). Toggle added to PI-5's pod edit form (`PodPage.tsx`'s `EditPodForm`, a checkbox below the field grid). Honored in `computeHallOfFame` (`hallOfFame.ts` — filters `excludeFromStats: false` at the pod query, replacing the comment that marked the spot) and the Treasure Chest rollup (`cardPulls.ts`'s `/api/card-pulls/treasure-chest`, same filter one hop through `pod`). The per-tournament Gesamtwertung and the pod's own standings deliberately do **not** filter on it — exclusion is only about the org-wide leaderboards, per plan.
- [x] Granularity: kept the single `excludeFromStats` flag — no real case yet for splitting standings-vs-value.
- [x] Verify — live against the running compose stack: created a tournament with one normal pod and one `excludeFromStats: true` "Joke Pod", played both to completion (real match results) and added a card pull to each. Confirmed Gesamtwertung includes both pods' points (Alice: 3+3=6, both `perPod` entries present). Confirmed Hall of Fame's `podsPlayed`/`totalPoints` only reflect the normal pod (Alice's total only grew by the Normal Pod's +3, not the Joke Pod's). Confirmed Treasure Chest lists only the normal pod's card pull, not the Joke Pod's. Full server test suite (7/7) still green.

### PI-7 — Rich player statistics: Hall of Fame overview + per-player pages (extends PI-3) ✅ (implemented 2026-08-24, not yet deployed)
- [x] Backend: new `server/src/services/playerStats.ts`. `buildLedger(orgId)` walks every non-excluded pod's rounds/matches once and produces (a) a per-player chronological match ledger (one entry per player per real match — a team match credits each member the team's full result once, not multiplied, matching the Gesamtwertung/Hall-of-Fame full-credit rule) and (b) pairwise head-to-head tallies (cross-product of team members on each side, same pattern `weekendHistory.ts` already uses for repeat-avoidance). `computeHallOfFameOverview()` and `computePlayerStats()` both build on this ledger plus the existing `computePodStandings`/`computeGesamtwertung`/`computeHallOfFame`.
- [x] `GET /api/hall-of-fame` now also returns `headline` (tournament/pod/player counts), `mostPlayedPairings` (top 5 pairs by matches together), and `biggestPulls` (top 5 org-wide, excluding `excludeFromStats` pods) — additive, the existing `hallOfFame` array is unchanged so nothing that already consumes it breaks.
- [x] New `GET /api/hall-of-fame/players/:id` (org-scoped, 404s cross-org): full record (matches/wins/losses/draws, match win %, game win %), pod wins + weekend wins (rank-1 finishes — confirmed the "finals" language doesn't apply to Swiss pods, used top-table finishes as planned), longest win streak, best format (min 3 matches in that format to qualify — a single lucky match in a format tried once isn't a signal), average finishing position, undefeated-pod count, total value pulled + biggest single pull, most-played opponent, nemesis/victim (min 2 head-to-head games to qualify), and the full head-to-head list.
- [x] **Scope decision, documented not hidden:** "pod wins" credits only `standings[0]` (the winner after the pod's full points+tiebreaker sort) — a genuine multi-way tie at rank 1 (identical points *and* every tiebreaker) credits only whichever entrant Prisma happened to return first, not all tied entrants. Full tie-detection was judged not worth the complexity for a friend-group-scale app; noted here rather than silently approximated.
- [x] **Real gap found and fixed before building on it:** `CardPull.playerId` was in the schema and accepted by the API but **no UI ever set it** — every real card pull in the app had `playerId: null`, so "total value pulled" per player would have shown €0 for everyone forever. Fixed by adding an optional "Pulled by…" select to `PodValuePage`'s add-pull form (`pullablePlayers()` resolves the pod's entrants to individual players, team members included), so the stat is real going forward. Verified live: attributed a pull to Alice via the API, confirmed her player-detail page picked up the real total and biggest-pull.
- [x] Frontend: `HallOfFamePage.tsx` extended with headline stat cards, a "most-played pairings" bar list, and a "biggest pulls" mini-list; each ranking row now links to `/hall-of-fame/players/:id`. New `PlayerStatsPage.tsx`: stat tiles for the full record list above, nemesis/victim cards, and a pure-Tailwind win/draw/loss stacked-bar chart of record vs. every opponent (`HeadToHeadChart`). New `usePlayerStats` hook alongside the existing `useHallOfFame`.
- [x] **Skipped, per the plan's own "only if genuinely curvy" charting guidance:** win%-over-time. It would need either a real date axis per match (this app tracks pod/round order, not per-match timestamps) or a charting dependency the plan explicitly said to avoid absent real justification — the bar chart (pure Tailwind, no deps) covers the "record vs each opponent" ask on its own.
- [x] Verify — live against the running compose stack, not just reasoning: confirmed `mostPlayedPairings`/`biggestPulls`/`headline` on a real multi-tournament, multi-pod dataset; confirmed a completed-round score edit and a fresh attributed card pull both flow through into the per-player page's live numbers; confirmed `GET /api/hall-of-fame/players/:id` 404s for a second org against the first org's player id, and the overview correctly returns empty for an org with no data. Screenshotted the overview and a player detail page — both render cleanly against the PI-10 theme. Full server test suite (7/7) still green; both client and server `tsc -b` clean.

### PI-8 — Tournament description with clickable links (shown on the public page too) ✅ (implemented 2026-08-24, not yet deployed)
- [x] `Tournament.description String?` (Postgres `TEXT`) added — migration `20260824130000_add_tournament_description`.
- [x] Backend: `description` in the tournament create/update zod schemas (`tournaments.ts`; nullable on update so it can be cleared, max 4000). Public endpoint returns it automatically (`findPublicTournament` is an unnarrowed `findFirst`).
- [x] Editing: description textarea in the create form (`DashboardPage`), plus an inline edit-in-place on `TournamentPage` (RichText view + "Edit description" → textarea + Save/Cancel) via a new `useUpdateTournament` hook — so it's editable now without waiting for PI-5's full tournament-edit form. New `Textarea` UI primitive.
- [x] Rendering + links: went with the **safe minimal** route — a `RichText` component (`client/src/components/RichText.tsx`) renders the blurb as **React nodes** (plain text auto-escaped), turning only `https?://…` URLs and `[label](url)` into `<a target=_blank rel=noopener noreferrer>`. No raw HTML, no `dangerouslySetInnerHTML`, so no XSS surface — and no new dependency (skipped the Markdown lib).
- [x] Displayed under the title on both `TournamentPage` (authed, editable) and `PublicTournamentPage` (public, read-only).
- [x] Deployed & verified 2026-08-24 (Tobias confirmed create/edit + public render + clickable links all work).

### PI-9 — Treasure Chest: per-card tournament label linking to the tournament ✅ (implemented 2026-08-24, not yet deployed)
- [x] On the Treasure Chest page (`/treasure-chest`, the org-wide most-valuable-cards gallery), each card shows a small label naming the **tournament** it was opened in, linking to that tournament (`/tournaments/:id`).
- [x] Data was already there: `GET /api/card-pulls/treasure-chest` includes `pod: { id, name, tournament: { id, name } }`, and the `CardPull` type carries `pod?.tournament` — no backend change needed.
- [x] `CardGallery` got an opt-in `tournamentLink` prop (default off, so the label stays out of pod value / tournament value / public views where it'd be redundant) — enabled only on `TreasureChestPage`. Renders a small `<Link to={/tournaments/${pull.pod.tournament.id}}>` under the price/set-code row.
- [x] Verify: `npm run --workspace client build` (`tsc -b && vite build`, run inside a local `node:22` container since this host has no system Node) passes clean — no type errors, build succeeds.
- [ ] Still to do: commit, deploy to daxlite, and eyeball it on a real page.

### PI-10 — Contrast/theme pass: lighter base text + a more vibrant accent (away from gold) ✅ (implemented 2026-08-24, not yet deployed)
- [x] Brightened the ink ramp in `client/src/index.css`'s `@theme` block — `--color-ink` #f2ede4→#f7f4ee, `--color-ink-secondary` #a89f8f→#bcb4a9, `--color-ink-muted` #8a8175→#aca59a. Computed WCAG contrast (not eyeballed): ink-secondary went 7.14:1→9.11:1 vs bg, ink-muted (the one that was borderline at 4.53:1 vs surface) went to 7.11:1 vs surface — real headroom, not just past the floor.
- [x] Accent hue picked with Tobias: **warm red-orange**. Swapped `--color-accent` #a67c27→#dc5e28, `--color-accent-strong` #c2953f→#e2784b, `--color-accent-wash` to `rgba(220, 94, 40, 0.14)` (plus the body background radial-gradient tint, same rgb). Verified every real use case clears 4.5:1: accent as text on bg (5.05:1) and on surface (4.69:1), and — the tightest case — the primary button's dark `#241c0a` text on the accent fill (4.56:1).
- [x] Ran the dataviz skill's palette validator (`validate_palette.js`) to re-check the specific concern the original code comment flagged (accent vs. the warning status color): ΔE (normal vision) is 20.7, comfortably clear of the 15 floor that the original gold was picked to satisfy (was ΔE 9.6, a fail).
- [x] Verified visually, not just by the numbers — screenshotted Dashboard, Gesamtwertung (rank-1 badge, hero average number), a pod's Entrants/Pairings/Standings tabs (primary "+ Add entrant" button, active-tab underline, leader-row gold-now-red-orange stripe) via the project's established Playwright-via-Docker-image fallback. Everything renders legibly against the dark theme with no regressions.

### PI-11 — Inferred card-pull attribution (requested 2026-08-24, implemented same day, not yet deployed) ✅
Motivation: PI-7's "total value pulled" per-player stat is real infrastructure with no real data behind it — `CardPull.playerId` was never set by any UI, historical or otherwise. Tobias's proposed heuristic: a top-3 pod finisher almost always picked one of that pod's most valuable cards, above a real price floor. Auto-apply that as a *guess*, clearly marked, until a human confirms or reassigns it.
- [x] Schema: `CardPull.playerIdInferred Boolean @default(false)` (migration `20260824164024_add_card_pull_inferred_flag`). Sticky false once a human sets or confirms an attribution — inference never overwrites that.
- [x] `server/src/services/cardPullInference.ts`: `inferCardPullAttribution(podId)` — only runs once a pod is fully decided (every round played and `COMPLETED`), skips team pods entirely (a card can't be credited to "a team," only one player, and guessing which member would be worse than not guessing). Zips `computePodStandings()`'s top 3 ranks against the pod's card pulls sorted by price descending, **positionally** (rank *i* only matched against price-rank *i*, not "top 3 cards" as a shifted group) — so if the pod's 2nd-priciest card is under the €10 floor, the runner-up gets no guess at all rather than a wrong one. Threshold is a strict `> €10`, matching Tobias's own wording.
- [x] Wired into three triggers so it's genuinely automatic, not a manual button: adding a card pull (`POST /pods/:id/card-pulls`), completing a round (`POST /rounds/:id/complete`), and correcting a completed round's result (`PATCH /matches/:id/result`, from PI-5) — the last one matters because a score correction can change who finished top-3, and any *still-unconfirmed* guess should track that; a confirmed one never moves again regardless.
- [x] New `PATCH /api/card-pulls/:id` (`{playerId: string | null}`) — the same endpoint both confirms an inferred guess as-is (pass back the same playerId) and reassigns it to someone else; either way it clears `playerIdInferred`.
- [x] One-time backfill script `server/src/scripts/infer-existing-card-pulls.ts` (mirrors `import-legacy.ts`'s standalone-script pattern) — runs the same heuristic across every already-completed pod, for pulls logged before this feature existed. Idempotent, safe to re-run, never touches a human-set/confirmed pull. Run once after deploying: `docker compose exec app node server/dist/scripts/infer-existing-card-pulls.js`. **Not run against the real historical data in this session** — deliberately, since `legacy-data.local.json` holds real names the earlier privacy correction scrubbed from git history; Tobias runs this himself against the real deployment.
- [x] Frontend: `CardGallery` shows a 🔮 marker on any inferred, unconfirmed pull; on `PodValuePage` specifically (the only page with `editableAttribution` set) an inferred pull gets an inline `<select>` (defaulting to the current guess) + a one-click confirm button instead of just the marker. Confirmed/human-set pulls show a plain player name, no marker. New `useSetCardPullAttribution` hook.
- [x] Verify — live against a running compose stack, not just reasoning: built a real 4-player pod, added 4 pulls (two above €10, two below) in an order that didn't match finish order, played it out, and confirmed the top-2 finishers got matched to the top-2 priced cards *positionally* while the two sub-€10 pulls stayed unattributed. Confirmed one attribution via the PATCH endpoint, then corrected the completed round's result to flip the standings order, and confirmed the **confirmed** pull stayed put while the **unconfirmed** one updated to the new rank-2 player. Confirmed the backfill script is idempotent (0 pulls touched when everything's already attributed) and correctly re-infers a pull that's been manually cleared back to null. Screenshotted the `PodValuePage` UI showing a confirmed pull (plain name), an inferred pull (🔮 + select + confirm button), and pulls below the floor (no player shown at all). Full server test suite (7/7) and both `tsc -b` builds still green.

### PI-12 — Longest win-streak spotlight on the Hall of Fame overview (requested 2026-08-24, implemented same day) ✅
Follow-up to a question about the per-player "longest win streak" stat (PI-7): confirmed it's genuinely career-spanning, not capped at a pod's round count (it's computed off the player's whole chronological match ledger, not reset at pod/tournament boundaries) — worth keeping, and worth surfacing org-wide.
- [x] Extracted the streak-counting loop out of `computePlayerStats` into a shared `longestWinStreakFor(matches)` helper in `playerStats.ts`, reused it in a new pass over `computeHallOfFameOverview()` that finds the single highest streak across every player in the org.
- [x] `HallOfFameOverview.longestWinStreak: {playerId, displayName, streak} | null` — additive on `GET /api/hall-of-fame`, same pattern as the other overview extras.
- [x] Frontend: a 4th tile in `HallOfFamePage`'s headline stats row (accent-highlighted, 🔥 + streak count + player name), clickable through to that player's stats page.
- [x] Verify: live API check confirmed the field appears and picks the correct max; screenshotted the rendered tile.

### PI-13 — Public org-wide Hall of Fame + Treasure Chest links, plus two real inference bugs found while testing (requested 2026-08-24, implemented same day) ✅
Motivation: the app already had shareable public links per-tournament and per-pod (`/o/:slug/...`), but nothing org-wide — no way to share "the whole group's Hall of Fame" the way you can share one weekend. Same trust model extends cleanly: the org slug alone is already the entire access control for the public surface, so an org-wide page needs no new concept, just no tournament/pod id in the URL.
- [x] Backend: `findPublicOrganization(slug)` (`ownership.ts`), three new routes in `public.ts` — `GET /api/public/o/:slug/hall-of-fame`, `.../hall-of-fame/players/:playerId`, `.../treasure-chest` — thin wrappers around the same `computeHallOfFameOverview`/`computePlayerStats` services the authed routes already use, scoped by the resolved org id instead of `request.organizer.orgId`.
- [x] Frontend: extracted `HallOfFamePage.tsx`'s subcomponents into a shared `components/HallOfFameOverview.tsx` (`HeadlineStats`, `HallOfFameList`, `MostPlayedPairings`, `BiggestPulls`, each taking a `playerLinkTo` builder instead of a hardcoded path) and `PlayerStatsPage.tsx`'s body into `components/PlayerStatsBody.tsx` — same pattern the app already used for `GesamtwertungList`. Generalized `CardGallery`'s `tournamentLink` boolean into a `tournamentLinkTo` builder function for the same reason (authed vs public link targets differ). New `PublicHallOfFamePage`, `PublicPlayerStatsPage`, `PublicTreasureChestPage` routes at `/o/:slug/hall-of-fame`, `/o/:slug/hall-of-fame/players/:id`, `/o/:slug/treasure-chest` — same rich content as the authed pages, zero duplicated JSX. "Public link ↗" added to the authed `HallOfFamePage` and `TreasureChestPage`, matching the existing pattern on `TournamentPage`/`PodPage`.
- [x] **Real bug found from a user report, not a review pass**: the per-player "total value pulled" stat was showing empty for every real player. Root cause was actually two things layered together, only one of which was a code bug — (1) the real historical pods were imported via `import-legacy.ts`, which writes to the database directly and never touches the HTTP routes PI-11's inference is wired into, so none of them were ever auto-inferred — expected, that's exactly why the one-time backfill script exists, it just hadn't been run yet; (2) the actual bug: `inferCardPullAttribution`'s "is this pod decided?" check only recognized pods with completed `Round`/`Match` data, so any pod imported as standings-only (final points recorded, no round-by-round data — most of the pre-app history, deliberately, per the import script's own "don't fabricate pairings that were never recorded" design) could never be inferred, full stop, no matter how many times the backfill ran. Fixed by recognizing a second "decided" condition: every entrant has `finalPointsOverride` set. Confirmed after the fact that this particular user's real data no longer has standings-only pods (fixed in a separate session), but the fix stands as a real gap for anyone else's import.
- [x] **Second request from the same conversation**: a per-player card-pull gallery, "like the overview's biggest-pulls list but filtered to that player," at the bottom of the player detail page. `computePlayerStats` now also returns the player's full `cardPulls` list (reusing the same query that already computed `biggestPull`, rather than a separate lookup); `PlayerStatsBody` renders it through the same shared `CardGallery` component, `tournamentLinkTo`-aware so it works on both the authed and public player pages.
- [x] Verify — live against a running compose stack: confirmed all three new public endpoints return real data with zero cookies sent, confirmed a wrong org slug and an unknown player id both 404, confirmed no authenticated chrome (nav, "Log out") leaks onto the public pages. Screenshotted the public Hall of Fame overview (headline stats incl. win-streak tile, ranked list, most-played pairings, biggest pulls — full parity with the authed page), a public player detail page (all stat tiles plus the new card-pull gallery), and the public Treasure Chest page. Full server test suite (7/7) and both `tsc -b` builds still green.

### PI-14 — Main-event flag + Hall of Fame crowns (requested 2026-08-24, implemented same day) ✅
Motivation: this group's Chaos Draft is a special, standout event each weekend — wanted it (and only it) called out on the Hall of Fame with a crown per win, clickable to the pod. Generalized rather than hardcoding the format: any pod can be checked as "the main event" for its tournament, one per tournament.
- [x] Schema: `Pod.isMainEvent Boolean @default(false)`, migration `20260824180541_add_pod_main_event` — hand-added a **partial unique index** (`CREATE UNIQUE INDEX ... ON "Pod"("tournamentId") WHERE "isMainEvent" = true`, since Prisma's schema DSL can't express a WHERE-qualified unique constraint) as the real guarantee against two main-event pods ever coexisting in one tournament, on top of app-layer code that unsets any sibling first. Verified with `prisma migrate status` that hand-editing the generated migration didn't cause drift.
- [x] `podRoutes` (create + update): setting `isMainEvent: true` runs in a transaction that first unsets any other pod in that tournament — belt-and-suspenders with the DB constraint, not a replacement for it.
- [x] `computeHallOfFame` (`hallOfFame.ts`) now also computes `mainEventWins: MainEventWin[]` per player row, reusing the same per-pod `computePodStandings()` loop that already ran for the ranking itself — no extra queries. Whoever's `standings[0]` in a main-event pod gets credited (same rank-1-after-full-tiebreak rule, same team-full-credit rule, same documented single-winner-on-tie simplification already used elsewhere in this codebase). **Crowns are computed live from the pod's current `isMainEvent` flag, not stored** — un-marking a pod as the main event removes any crown it had granted, confirmed live.
- [x] Frontend: checkbox ("Mark as this tournament's main event…") on both the pod-create form (`TournamentPage`) and the pod-edit form (`PodPage`), plus a 👑 badge next to the pod's name wherever it's shown (tournament's pod list, the pod page's own title). `HallOfFameList` (shared, PI-13) renders one crown per win under the player's name, each independently linking to its own pod.
- [x] **Real UI problem solved, not glossed over**: a crown needs to link to its own pod, but the whole Hall of Fame row is already one big `Link` to the player's page — can't nest a `<Link>` inside a `<Link>` (invalid HTML). Fixed with the standard "full-card overlay link" pattern: the row became a `<div>` with an absolutely-positioned `<Link to={player}>` covering it and `pointer-events-none` on the visible content so clicks fall through to that overlay everywhere *except* the crowns, which sit in their own `pointer-events-auto` layer and navigate independently. Verified by actually clicking a crown in a live browser and checking the resulting URL, not just reading the JSX.
- [x] Verify — live against a running compose stack: created a tournament with a main-event pod and a second pod, played the main-event pod to a real completion, confirmed the winner got a crown in `GET /api/hall-of-fame`'s response. Marked the second pod as main event instead and confirmed (a) the first pod's `isMainEvent` flipped false automatically, (b) the crown disappeared from the standings response (second pod has no winner yet). Screenshotted the rendered crown, then drove a real click on it and confirmed the browser navigated to `/pods/:id` (the pod), not the player's page. Full server test suite (7/7) and both `tsc -b` builds still green.

### PI-15 — Card-pull printing accuracy: set-specific resolution, foil pricing, per-pod default set (requested 2026-08-24, implemented same day) ✅
Motivation: syncing a real pod's pulls from a Moxfield decklist (via the MCP server) surfaced that `add_card_pull`'s Scryfall resolution (`lookupCardByName()` in `server/src/services/scryfall.ts`, `GET /cards/named?fuzzy=`) has no way to select a specific printing — it always returns Scryfall's own "default" printing for a card name. That's silently wrong for any card reprinted across multiple sets: syncing a real Edge of Eternities (EOE) pod, 6 of 28 cards resolved to unrelated sets (Star Trek, Ultimate Masters, Time Spiral Remastered, Oath of the Gods, Iconic Masters) instead of `eoe`/`eos`. There's also no way to fix a wrong pull after the fact short of delete + recreate, which loses `playerId`/`addedAt`. Two related asks folded in: a per-pod default set (pre-fills the printing picker) and foil pricing (foil vs. nonfoil price differs significantly and isn't tracked at all today).
- [x] `scryfall.ts`: `lookupCardByName(name, options?: {setCode?, foil?})` passes Scryfall's `set` query param through when given. No silent fallback to a different printing when the name doesn't exist in that set — a real, not-found set/name combination returns `null` (→ `card_not_found`), exactly the same as today's "card doesn't exist at all" case. Cache key now includes `setCode`+`foil` so the two never collide.
- [x] `cardPulls.ts`: `addPullSchema` gains optional `setCode` and `foil` (default `false`), threaded into `lookupCardByName`.
- [x] **Scope decision**: rather than a new nested `PATCH /api/pods/:id/card-pulls/:pullId` route, extended the existing flat `PATCH /api/card-pulls/:id` (already built in PI-11 for attribution confirm/reassign) to also accept `setCode`/`foil` — one endpoint that can update either or both independently in a single call, instead of two near-duplicate PATCH routes for the same resource. Re-resolves via Scryfall and overwrites `scryfallId`/`setCode`/`foil`/`priceEur`/`imageUri` in place; a failed re-resolution (bad set/name combo) leaves the existing row untouched and 404s rather than clobbering good data.
- [x] MCP (`mcp/src/index.ts`): `add_card_pull` gained optional `setCode`/`foil` params; new `update_card_pull` tool wrapping the extended PATCH above.
- [x] Schema: `Pod.setCode String?`, migration `20260824184644_add_pod_setcode_and_cardpull_foil`. Editable on both pod forms via a new shared `SetPicker` component, shown only when `format` is `DRAFT` or `SEALED`.
- [x] Backend: `listMainSets()` in `scryfall.ts` — `GET /sets`, filtered to `expansion`/`core` and non-digital, sorted newest-first, cached 24h. New `GET /api/scryfall/sets` route.
- [x] Frontend: `SetPicker` (dropdown, populated from the cached sets list) on both pod forms; the add-pull form (`PodValuePage`) gets a free-text set field (not constrained to the "main sets" list, since it needs to support the bonus-sheet override case below) pre-filled from the pod's `setCode` but editable per pull.
- [x] Schema: `CardPull.foil Boolean @default(false)`, same migration as `Pod.setCode` above.
- [x] `scryfall.ts`'s `resolvePrice()`: picks `eur`/`eur_foil` based on the requested finish, but only if the print's `finishes` array actually has that finish — falls back to whichever finish the print *does* have (verified live: a foil request against Limited Edition Alpha's foil-less Lightning Bolt correctly fell back to the nonfoil price rather than storing null) and reports which finish the stored price actually reflects, rather than blindly echoing back what was requested.
- [x] Frontend: foil checkbox on the add-pull form; `CardGallery` shows a ✨ badge on the card image plus "· foil" next to the set code for pulls that have it.
- [x] **Known gap, deliberately not auto-solved:** bonus-sheet/companion sets (e.g. EOE's "Stellar Sights" bonus sheet, code `eos`) aren't covered by a pod's single default `setCode` — the per-pull free-text override handles it instead, as originally planned; no linked-child-set auto-inclusion was built.
- [x] Verify — live against a running compose stack, not the real deployment: confirmed `setCode` correctly pins a specific printing (Lightning Bolt: default `msc` printing at €1.75 vs `setCode: "lea"` at €409.41 — the actual 1993 Alpha printing, a deliberately extreme case to make a resolution bug impossible to miss); confirmed a set that doesn't print the card 404s with no fallback; confirmed foil pricing picks the real `eur_foil` value when a finish exists (€1.75 nonfoil vs €1.66 foil on the same modern printing) and falls back correctly when it doesn't; confirmed the `PATCH` re-resolution fixes a wrong pull in place and a failed re-resolution leaves it untouched; drove the same three operations through the real MCP `add_card_pull`/`update_card_pull` tools over the actual stdio protocol, including a clean tool-level error (not a crash) on a bad set code. Screenshotted the pod edit form's set picker and the Value tab's set/foil fields plus a rendered pull. Full server test suite (7/7), and all three `tsc -b`/mcp typechecks, still green.
- [ ] **Not done, deliberately**: the checklist's final verify step calls for re-running the real 6-card EOE correction against the live "Edge" pod (`cmt70fub800jmqo3yv9qiarke`) via `update_card_pull`. That requires a real API token and network access to the actual deployed instance, neither of which this session has (or should assume) — per this repo's own convention, touching live infra is Tobias's call, not something to do unannounced from here. The feature is fully built and verified against equivalent synthetic cases above; running the real correction is a `docker compose up -d --build` + mint-a-token + six `update_card_pull` calls away, whenever Tobias wants to do it himself (or asks for it explicitly).

### PI-16 — Manual tiebreak override for intentional-draw endgames (requested 2026-08-24, implemented same day) ✅
Motivation: players sometimes intentionally draw their final match to lock in placement over a player below them (e.g. 2W-1D = 7 points, safely clear of 3rd place's 6) — a deliberate human decision, not something the computed OMW%/GW%/OGW% tiebreakers necessarily reflect, and in a small pod those tiebreakers can easily tie too, leaving the two drawers in an arbitrary array order. Directly relevant to PI-14's crowns and PI-7's pod-wins/inference, all three of which trust `standings[0]` as "the winner" — flagged at the time as a known gap this would eventually need to fix.
- [x] Schema: `Entrant.manualTiebreak Int?`, migration `20260824182221_add_entrant_manual_tiebreak`. Deliberately narrow scope: only ever compared between entrants who are **already tied on points** — it can never move an entrant ahead of someone with more points, so there's no way to use it to fabricate a result that didn't happen.
- [x] `computePodStandings`'s sort (`standings.ts`) now checks `manualTiebreak` **before** the computed OMW%/GW%/OGW% tiebreakers (not after) whenever either tied entrant has one set — a manual call is meant to override the math, not just serve as a last-resort when the math also ties. Every consumer of `computePodStandings` (pod wins, card-pull inference, Hall of Fame crowns, Gesamtwertung) picks this up automatically since none of them re-implement sorting.
- [x] New `PATCH /api/entrants/:id` (`{manualTiebreak: number | null}`), org-scoped via a new `findOwnedEntrant` helper (`ownership.ts`) — added for consistency and reused to simplify the existing `DELETE /entrants/:id` handler too.
- [x] Frontend: `PodStandingsPage` gets ▲/▼ arrows on any row tied on points with its neighbor, swapping the pair's `manualTiebreak` values (reusing their current display indices — the values only ever matter relative to each other, they don't need to be clean sequential numbers); a "✋ manual" badge on any row with an override set, plus a "✕" to clear it back to fully-computed ordering. New `useSetManualTiebreak` hook.
- [x] Verify — live against a running compose stack: built a real 4-player, 2-round pod engineered so two pairs end up tied (Alice/Carol at 4 points with *identical* tiebreakers too — the exact "nothing left to compute from" case), confirmed the API returns them in arbitrary order by default, set `manualTiebreak` to swap Carol above Alice, and confirmed the standings response immediately reflected the new order while the untouched Bob/Dave tie was unaffected. Confirmed cross-org 404 on the new PATCH route. Drove the actual UI in a browser: screenshotted the tied rows with their arrows, clicked "move down" on the top row, and screenshotted the result showing the swap took effect with both rows now labeled "✋ manual." Full server test suite (7/7) and both `tsc -b` builds still green.

### PI-17 — Security hardening for public exposure via Cloudflare (rate limiting, closed signup, headers, trusted proxy, anti-scraping), plus two real bugs found in the process (requested 2026-08-24, implemented same day) ✅
Motivation: sharing this with a friend group means making it internet-reachable (via Cloudflare Tunnel), which is a different threat model than LAN-only — needed baseline protections that hold up even without Cloudflare in front (self-hosters won't all use it), while keeping the public org/tournament/pod pages exactly as frictionless as they are today (no login wall — the org slug is the intended access control, not a secret to be locked down further).
- [x] **Rate limiting.** `@fastify/rate-limit` registered globally in `index.ts` (200 req/min, generic abuse ceiling), with tighter per-route limits on the two endpoints actual brute-force/spam targets: `/api/auth/login` (10/min) and `/api/auth/signup` (5/10min), via the 3-arg `app.post(path, {config: {rateLimit: {...}}}, handler)` form.
- [x] **Signup gated behind `ALLOW_SIGNUP`.** `config.allowSignup` (env-driven, `false` by default — this app is for an existing group, not an open signup form). New public `GET /api/auth/signup-status` so the frontend can show a clear "closed" message instead of a dead-end form; `POST /api/auth/signup` 403s with `signup_disabled` when closed. `docker-compose.yml` + `.env.example` updated; toggling it is a one-line env change + restart, no redeploy. Frontend: `useSignupStatus` hook, `SignupPage` renders a closed-state card instead of the form when disabled, plus a 403-specific error message if a race lets someone submit right as it closes.
- [x] **Security headers.** `@fastify/helmet` with a CSP scoped to what this app actually loads (`'self'` scripts/styles, `cards.scryfall.io` for card images, same-origin fetch/WebSocket, `frameAncestors: 'none'` against clickjacking) and conditional `hsts`/`crossOriginOpenerPolicy` (only once `SESSION_COOKIE_SECURE` says the app is actually behind TLS — see the real bug below for why "conditional" was the wrong instinct to skip).
- [x] **Trusted proxy.** `trustProxy: true` on the Fastify instance, so `request.ip` (and therefore rate-limit's per-IP bucketing) reflects the real client IP from `X-Forwarded-For`/`CF-Connecting-IP` when Cloudflare Tunnel is in front, instead of bucketing every visitor together under the tunnel's own address.
- [x] **Anti-scraping/indexing, no login wall added.** Global `X-Robots-Tag: noindex, nofollow` response header (binding at the indexing stage, catches links crawlers reach without ever fetching `robots.txt`) plus `GET /robots.txt` (`Disallow: /`, covers well-behaved crawlers at the crawl stage). Public routes themselves are unchanged — this is intentionally the ceiling of what HTTP-layer signals can do without adding friction real visitors would also hit.
- [x] **Real bug found + fixed: helmet's default CSP breaks plain-HTTP deployments.** Helmet's `contentSecurityPolicy` ships `upgrade-insecure-requests` as a default directive — on a server that only speaks HTTP (LAN-only, or behind a tunnel that terminates TLS upstream, which `.env.example`'s `SESSION_COOKIE_SECURE=false` explicitly supports), that directive tells the browser to rewrite every `http://` asset/API request to `https://` before sending it, which the server can't answer — every asset fails `ERR_SSL_PROTOCOL_ERROR` and the SPA never mounts (blank page, no visible error in the UI itself). This typechecks fine and only breaks at runtime — caught by actually loading the app in a real (Playwright) browser after adding the CSP, not by `tsc` or the test suite. Fixed by explicitly disabling it (`upgradeInsecureRequests: null`) unless `SESSION_COOKIE_SECURE` says the deployment is really behind TLS, same conditional already used for `hsts`. `crossOriginOpenerPolicy` given the same treatment (console warning, not breakage, but same root cause).
- [x] **Sets list fix.** `listMainSets()` was filtering to `set_type` `expansion`/`core` only, which silently dropped real drafted product: Masters-line sets (`masters` — Mystery Booster 2, Ultimate Masters, the "X Remastered" sets) and special draft-format sets (`draft_innovation` — Modern Horizons 1/2/3, Jumpstart, Battlebond, Conspiracy, Commander Legends). Verified the actual `set_type` values against live Scryfall data rather than assuming — also surfaced that the original 2019 Mystery Booster's cards are filed under Scryfall's `plst`/"The List" internally, not under a set literally named "Mystery Booster." Broadened `MAIN_SET_TYPES` to `{expansion, core, masters, draft_innovation}`; still excludes commander/promo/token/box product (available via the existing free-text override, just not offered as a default). Result: 182 clean sets, confirmed live via the authenticated `GET /api/scryfall/sets` response.
- [x] **Set picker UX: browsable/alphabetized list instead of a bare text input.** `PodValuePage`'s add-pull set field is now a `<datalist>`-backed combobox sourced from `useScryfallSets()`, alphabetized by name (a different order than the sets-picker dropdown elsewhere, which is newest-first — this one's for finding a specific set by name, not "what's recent"). Preserves the free-text fallback PI-15 deliberately built in for bonus-sheet sets not in the "main sets" list: typing a name resolves to its code via a name→code map, typing anything that doesn't match a known name passes through as-is.
- [x] **Scryfall caching, answered.** A card pull's price/image is a permanent snapshot taken at add-time and never re-fetched on view — browsing pulls does zero Scryfall calls. The live calls (autocomplete while typing, resolving a new pull, the sets list) already had in-memory TTL caching from PI-15 (5 min / 1 hr / 24 hr respectively) — no new caching needed for this. The one real gap: that cache is in-memory only and resets on every container restart/redeploy, so there's a small burst of re-fetching right after a deploy — not sustained "hammering" at this app's traffic scale, not worth disk/DB-backed persistence unless that changes.
- [x] Verify — live against a rebuilt compose stack, not just typecheck: confirmed via `curl` that CSP/HSTS/robots headers are present on every response, `robots.txt` serves `Disallow: /`, `signup-status` reflects `false` by default and `POST /auth/signup` 403s with `signup_disabled`; hammered `/api/auth/login` 12x within a minute and confirmed requests 11-12 came back `429` (10 came back `401`, the expected wrong-password response). Full server test suite (7/7) run against the compose `db` (needed `--network` + `--env-file` wiring since tests need a live Postgres, not mocked). Drove a full real browser session (Playwright) through signup → login → pod value page: confirmed the CSP fix actually fixes the blank-page bug (SPA mounts and works), confirmed the set picker shows all 182 sets alphabetized, and added a real card pull (Lightning Bolt) to confirm its Scryfall image loads under the new `imgSrc` CSP allowance. All throwaway verification orgs deleted afterward; `ALLOW_SIGNUP` restored to `false` in `.env`.

### PI-18 — Actually finish PI-13: one real org-wide public link, not five separate ones (requested 2026-08-24, implemented same day) ✅
Motivation: user feedback after living with PI-13 for a bit — "we messed up on PI-13." What got built was five independent shareable links (per-tournament, per-pod, org Hall of Fame, org Treasure Chest) each fully self-contained, which is useful but not what was actually wanted: **one** link (`/o/:slug`) to send a friend group that lets them browse everything read-only, the way an organizer does when logged in — not a pile of separate links for each sub-page. The gap was structural: `PublicLayout` had no nav (just a static logo/title bar) and there was no landing page at the bare `/o/:slug` root at all — visiting it fell through to the authed catch-all route and bounced to `/login`.
- [x] **Root cause, not a UI tweak**: `main.tsx`'s public routes were siblings under a pathless layout route, each independently declaring its full `/o/:slug/...` path — so `PublicLayout` itself was never rendered at any path containing `:slug`, meaning `useParams().slug` inside it would never resolve. Restructured to nest everything under one `<Route path="/o/:slug" element={<PublicLayout />}>` with relative child paths (`index`, `roster`, `tournaments/:id`, etc.) — the correct React Router shape for "a layout that needs a param its children share," and incidentally removes five repeats of the `/o/:slug/` prefix.
- [x] Backend: two new routes — `GET /api/public/o/:slug` (org name + tournament list, mirrors authed `GET /api/tournaments`) and `GET /api/public/o/:slug/roster` (mirrors authed `GET /api/players`) — both thin org-scoped wrappers, same trust model as every other route in `public.ts` (slug alone is the access control).
- [x] Frontend: new `PublicDashboardPage` (org landing page — tournament list, no create form) and `PublicRosterPage` (player list, no add/rename/remove), both read-only mirrors of their authed counterparts. `PublicLayout` rewritten with a real nav (Tournaments/Roster/Hall of Fame/Treasure Chest, mirroring the authed `Layout`'s nav) plus the org name, now that it can actually read `slug`. Deliberately **not** mirrored: no "API Tokens" link, no Log out, no create/edit/delete anywhere under this layout — this is a read-only view of what the organizer sees, not the organizer's own account.
- [x] Turned out the pod- and tournament-level public pages (`PublicPodPage`, `PublicTournamentPage`) already inlined everything an organizer sees for that pod/tournament on one page each (pairings + standings + value; gesamtwertung + card values) — no separate standings/rounds/value sub-routes needed there, so this fix only had two real gaps to fill (org root, roster), not five.
- [x] `DashboardPage` (authed) gets a "Public link (share this with your group) ↗" pointing at `/o/:slug`, same pattern PI-13 already used on `HallOfFamePage`/`TreasureChestPage`/`TournamentPage`/`PodPage` — this is now the one link an organizer actually hands out.
- [x] Verify — live against a rebuilt compose stack: confirmed both new endpoints return real data and a bad slug 404s. Drove a full Playwright session starting from the bare `/o/verify-org-a` root (the exact link-sharing scenario) through every nav link — Tournaments → Roster → Hall of Fame → Treasure Chest → back to Tournaments → into a real tournament — confirming each nav click lands on the right URL with zero console errors, and confirmed no authed chrome ("Log out", "API Tokens") ever appears on any of these pages. Screenshotted the org root and roster pages. Full server test suite (7/7) and both `tsc -b`/vite builds still green.
- [x] **Follow-up same day**: user feedback on PI-17's sets fix — Mystery Booster was still missing from the pod-level "Default set" picker (`SetPicker.tsx`), and asked to drop the curated `set_type` filter entirely in favor of every paper set from 2000 onward, alphabetized. `listMainSets()` (`scryfall.ts`) rewritten: dropped `MAIN_SET_TYPES` (had already needed broadening twice in one day — expansion/core, then masters/draft_innovation — and still missed things), now just `!digital && releasedAt >= "2000-01-01"`, sorted by name instead of release date. Result: 911 sets (up from 182), confirmed live via the authenticated endpoint — alphabetical order, no pre-2000 leakage, and "The List" (Scryfall's actual internal name for 2019's Mystery Booster, per PI-17's earlier finding) present. `PodValuePage`'s add-pull combobox no longer needs its own client-side alphabetical re-sort now that the server already returns one — simplified back to a plain derived array. `SetPicker.tsx` (the pod-level default-set dropdown) needed no code change — it was already just rendering whatever order the API returned, so it inherited the alphabetized list for free. Full server test suite (7/7) and both `tsc -b`/vite builds still green.

### PI-19 — Manual unassign for wrong inferred card-pull attribution (requested 2026-08-24, implemented same day) ✅
Motivation: some historical pods have card pulls logged but no round/match data at all (never recorded, not even standings-only) — yet PI-11's inference still auto-guessed the top-3 most valuable pulls onto a player, because the guess only needs *some* signal (pod entrants + pull values), not real placement data. With nothing to actually confirm the guess against, the organizer needed a way to say "no, unassign this" rather than being stuck reassigning to a different equally-unfounded guess or leaving a wrong 🔮 guess in place.
- [x] Backend needed **no changes** — `PATCH /api/card-pulls/:id`'s `playerId` field was already `z.string().min(1).nullable().optional()` (built in PI-15 for the setCode/foil correction flow) and already handled `playerId: null` correctly, clearing `playerIdInferred` too. The gap was entirely in the frontend never sending it.
- [x] `CardGallery.tsx`: renamed `InferredAttribution` → `AttributionEditor` and widened its scope — it now also handles a pull with **no** player at all (not just an unconfirmed inferred guess), since unassigning has to lead somewhere reassignable rather than a dead end. An "✕ Unassign" button appears next to "✓ Confirm/Assign" whenever there's a current `playerId` to clear; the select gains a "— unassigned —" option. Render condition changed from `pull.player && pull.playerIdInferred` to `pull.playerIdInferred || !pull.player`, so a freshly-unassigned pull immediately re-renders into "assign" mode instead of just disappearing.
- [x] `onSetAttribution` prop widened from `(pullId, playerId: string) => void` to `(pullId, playerId: string | null) => void` — `useSetCardPullAttribution` and `PodValuePage`'s wiring already accepted `null` end-to-end (only the UI layer needed the type widened), so this was pure plumbing.
- [x] Verify — live against a rebuilt compose stack: seeded a pod with zero rounds/matches (reproducing the user's exact "no real data" scenario) and a card pull manually flipped to `playerIdInferred: true` in the DB to simulate an inference guess. Drove the real browser flow: confirmed the ✕ button PATCHes `playerId: null` and the card re-renders into assign mode (no player shown, "— unassigned —" selected, no 🔮). Then, with an entrant actually in the pod, confirmed the reverse direction too — selecting a player and clicking "Assign" correctly attributes it as human-confirmed (no 🔮 badge, since `playerIdInferred` clears on any explicit `playerId` write). Full server test suite (7/7) and both `tsc -b`/vite builds still green.

### PI-20 — Bug: transient Scryfall errors get cached as permanent "card not found" (found 2026-08-24 syncing a real pod's pulls via MCP, fixed same day) ✅
Motivation: bulk-adding ~63 card pulls to a real pod via the MCP `add_card_pull` tool (no `setCode` passed) worked for the first ~20 calls, then every subsequent call — including trivially real card names like "Lightning Bolt" — started failing `card_not_found`, and stayed broken for 15+ minutes. Confirmed live that Scryfall itself was healthy the whole time (`curl https://api.scryfall.com/cards/named?fuzzy=lightning+bolt` → 200) and that the failing app requests were resolving in ~6ms — far too fast to be a real outbound HTTPS round-trip, meaning they were cache hits on a bad cached value, not live (still-failing) Scryfall calls. (Renumbered from PI-19 — collided with the same-day manual-unassign entry above, written in a separate concurrent session against this same repo.)
- [x] Root cause: `lookupCardByName()` (`server/src/services/scryfall.ts`) — `if (!res.ok) { setCached(key, null, NAMED_TTL_MS); return null; }` treated **any** non-2xx Scryfall response (a transient `429` from a burst of near-simultaneous requests, a `5xx`, etc.) exactly the same as a genuine "this card doesn't exist" `404`, and cached the negative result for the full `NAMED_TTL_MS` (1 hour), keyed on `name+setCode+foil`. One rate-limited burst poisoned every card name it touched for an hour — including names that would resolve fine on a normal retry.
- [x] Fix, two parts: (1) `scryfallFetch()` now retries once on `429`/`5xx` after a short backoff (honors a `Retry-After` header if Scryfall sends one, else 750ms) — absorbs a rate-limited burst before it ever reaches a caller as a failure at all. (2) `lookupCardByName()` now only caches a negative result when the (post-retry) response is actually a `404` — a `429`/`5xx`/network error that survives the retry returns `null` uncached, so the very next lookup of that same card tries Scryfall fresh instead of replaying a stale failure for an hour.
- [x] Regression test added (`server/src/services/scryfall.test.ts`, mocks `fetch` — no real Scryfall calls, no DB): confirms a `429` followed by a `200` resolves successfully via the retry; confirms a persistent `5xx` is never cached (two calls to `lookupCardByName` both hit `fetch`, not just the first); confirms a real `404` still caches correctly and unchanged (the second call makes zero additional `fetch` calls).
- [x] Real-world impact from the session that found this: 20 of 63 pulls landed on pod `cmt7lnn4g0003qi01db4mzxjv` (Mystery Booster Draft) before the poisoned cache blocked the rest; the remaining 43 still need adding (source: Moxfield deck `4qN9nRPBpkKyR5lp3o6reA` — full card list in that session's conversation). Unassigned to any player, per the request. **Not done here**: actually running that sync against the live deployed instance needs a real API token and network access to production, which this session doesn't have and shouldn't assume — per this repo's standing convention, touching live infra is Tobias's call. The code fix is verified and ready; finishing the sync is a `git pull` + rebuild + `update_card_pull`/`add_card_pull` ×43 away whenever Tobias runs it (or asks for it explicitly).
- [x] Verify — full server test suite (10/10, up from 7 — the 3 new scryfall tests) and `tsc -b server` clean.

### PI-21 — Manual pairing + swap-before-lock UI (requested 2026-08-25, implemented same day) ✅
Motivation: Phase C shipped auto-pairing only — the manual-pair and swap-before-lock APIs (`POST /pods/:id/rounds/manual`, `POST /rounds/:id/swap`) have existed since Step 4 and are already wrapped by the MCP tools (`manual_pair_round`, `swap_pairing`), but had no frontend, so an organizer without MCP access had no way to hand-pair a round or fix a pairing before it locks.
- [x] Backend needed **no changes** — both routes already existed, fully working, just never had a UI.
- [x] `useRounds.ts`: new `useManualPairRound(podId)` (`POST .../rounds/manual`) and `useSwapPairing(podId)` (`POST /rounds/:id/swap`) hooks; `roundErrorMessage` extended for `invalid_pairing`/`round_locked`/`cannot_swap_bye`/`no_op`.
- [x] **Manual pairing** (`PairingsPage.tsx`): a "Pair manually instead" link next to the existing auto-pair button opens `ManualPairingForm` — one dropdown-pair row per expected table (`Math.ceil(activeEntrants/2)`), each side's dropdown filtering out entrants already picked in any other row so a duplicate pairing is impossible by construction, not just caught after submit. Bye is "— Bye —" in the B-side dropdown. Active entrants computed client-side the same way the backend's `getActiveEntrants` does (`droppedAfterRound === null || droppedAfterRound >= nextRoundNumber`) — `Entrant.droppedAfterRound` was already exposed to the frontend, just unused for this.
- [x] **Swap-before-lock** (`PairingsPage.tsx`): while a round is `PENDING`, every entrant name becomes a clickable seat (`RoundCard` tracks one `SwapSlot | null` — `{matchId, side}` — as local state); clicking a second seat fires `useSwapPairing` with both slots and clears the selection on settle, clicking the same seat again cancels. A hint line explains the interaction; the currently-selected seat gets a ring highlight. Swapping is unavailable once a round is `ACTIVE`/`COMPLETED` (matches the backend's own `round_locked` check on non-`PENDING` rounds), and only entrant seats render as clickable — a bye slot never does, since the backend rejects swapping into/out of one anyway.
- [x] Verify — live against a rebuilt compose stack: seeded a real 4-entrant pod (Alice/Bob/Carol/Dave), used the manual pairing form through the actual UI to publish Table 1 = Alice/Bob, Table 2 = Carol/Dave (confirmed `201` on `/rounds/manual`), then clicked Bob then Carol to swap them (confirmed `200` on `/rounds/:id/swap`) and confirmed on a **fresh page load** (not just client-side state) that the swap persisted server-side: Table 1 = Alice/Carol, Table 2 = Bob/Dave. Screenshotted the manual pairing form (empty and filled) and the post-swap state. Full server test suite (10/10) and both `tsc -b`/vite builds still green.

> **PI-22 … PI-74 were relocated here from `ROADMAP.md` on 2026-09-06** once they were all shipped and browser-verified, to keep the roadmap scoped to open work. Text is as it stood in the roadmap at move time. PI-39 (legacy-import follow-up still open) and PI-62 (deck photos, not started) stayed behind in `ROADMAP.md`, so the numbering skips them here.

### PI-22 — Publish a real GitHub release (not just the rolling `main`) ✅
The repo is public on GitHub but had no tagged release: no `version` in any `package.json`, `git tag -l` empty, no GitHub Release with notes.
- [x] **Version scheme:** semver, starting at `v0.1.0`, single shared version (root `package.json` only — `server`/`client`/`mcp` stay unversioned).
- [x] Release notes drafted, tag pushed, GitHub Release created by Tobias: https://github.com/TobiasDax/LimitedGauntlet/releases/tag/v0.1.0.
- [x] GHCR image publish: `.github/workflows/docker-publish.yml`, triggered on `release: published` (+ manual `workflow_dispatch`), pushes `ghcr.io/<owner>/<repo>:0.1.0`, `:0.1`, and `:latest`.
- [x] **Forgejo→GitHub mirror gotcha, worth remembering:** `origin` here is Forgejo, not GitHub, so the tag had to actually propagate through the push-mirror before `gh release create` could see it, and `gh` needed `--repo TobiasDax/LimitedGauntlet` since it can't infer a GitHub repo from a Forgejo remote.

### PI-23 — Bug: Hall of Fame crown badge renders above the header menu ✅
The 👑 crown badge on the Hall of Fame scrolls *over* the top-bar menu instead of under it — a z-index / stacking-context issue, so on scroll the crown overlaps the nav. Low severity, but fix down the road.
- [x] Root cause: the crown container was `relative z-10` (`HallOfFameOverview.tsx`) and the sticky header was also `z-10` (`Layout.tsx`) — equal z-index, crown later in the DOM, so it painted over the header. Fix: crown lowered to `z-[1]` (it only needs to beat the row's overlay link) and the sticky header raised to `z-30` (now via the shared `TopBar`), so the header always wins over content.
- [x] Verified by scrolling a real Hall of Fame page with crowns in the viewport, both authed and public.

### PI-24 — Active-page indicator in the nav + keyboard/focus accessibility ✅
The top-bar menu has no visual indicator of the current page. Add a modern active state, and while in there, make the nav properly usable for keyboard / assistive-tech users.
- [x] Active nav item: **bold + underline in the accent color** via `NavLink`'s `isActive`, in the shared `TopBar` used by both `Layout` and `PublicLayout`. Home links use `end` so `/` (and `/o/:slug`) aren't active on every route.
- [x] Keyboard/a11y: `focus-visible` outline ring (accent) on every nav item and the hamburger; `NavLink` emits `aria-current="page"` automatically. Focus ring uses the accent, which clears contrast on the dark bar.
- [x] Verified keyboard tabbing + screen-reader `aria-current` in a real browser.

### PI-25 — Hamburger menu on mobile ✅
Alongside the PI-24 nav work, collapse the top bar into a hamburger menu on small screens so it's smaller and easier to navigate on a phone.
- [x] Below `sm:` the nav + right slot collapse into a hamburger toggle (`TopBar`) that drops a full-width menu panel; the wordmark + log-out stay reachable (log-out moves into the panel on mobile).
- [x] Accessible: toggle has `aria-label`/`aria-expanded`/`aria-controls`; menu closes on Escape, on outside click, and on selecting a link.
- [x] Verified the phone-width overflow fix (build log Step 9 Phase E) still holds, in a real 390px viewport.

### PI-26 — Organizer Settings page (shell + relocate API Tokens) ✅
Introduce a dedicated Settings area for organizer-scoped config. Right now only API Tokens would live there, but it declutters the top-bar nav and is the home for PI-27/PI-28.
- [x] New `SettingsPage` at `/settings` (inside the ProtectedRoute block) composing stacked `SettingsSection`s. API-token UI extracted to `components/settings/ApiTokensSection.tsx`; old `ApiTokensPage` deleted; `/api-tokens` now redirects to `/settings`.
- [x] Top-bar "API Tokens" link replaced with "Settings" (in the `Layout` right slot). Organizer-only via ProtectedRoute; the token routes it drives stay session-auth-only (unchanged).
- [x] `SettingsSection` wrapper is ready for PI-27/PI-28 sections to drop in.

### PI-27 — Optional password lock for public pages (Settings) ✅
Some organizers won't want fully-open public links. Let an organizer optionally set a password that gates their org's public (`/o/:slug/...`) pages. **Org-wide** (one password for the whole public surface).
- [x] Sanctioned exception to "public pages stay open by default" — `CLAUDE.md` softened accordingly. Off by default.
- [x] Schema: `Organization.publicPasswordHash String?` (migration `20260825110000_add_org_public_password`). Settings route (`settings.ts`, session-auth only, never bearer): `PUT /api/settings/public-lock` (argon2 hash) + `DELETE` to disable. `GET /auth/me` (and login) now return `publicLockEnabled`.
- [x] Public gating: an encapsulated `preHandler` on `publicRoutes` 401s `{error:"locked"}` for every data route when the org has a password and the visitor hasn't unlocked. Exempt: `GET .../lock` (status) and `POST .../unlock` (verify + mark unlocked in the encrypted session cookie; rate-limited 10/min like login). Slug stays shareable — the password is the gate, not secrecy of the URL.
- [x] Frontend: `PublicLockSection` in Settings (enable/change/disable). `PublicLayout` checks lock status first and renders `PublicUnlockPrompt` when locked+not-unlocked, so gated child queries never fire; unlocking invalidates the `["public"]` cache so everything refetches. Added `api.put`.
- [x] Live/browser verification done.

### PI-28 — Account management: change email / password, delete account (Settings) ✅
The organizer needs to manage their own account. Lives in the Settings page (`AccountSection`). All three sensitive actions verify the current password.
- [x] **Change password** — `POST /api/settings/password` (verify current, set new, immediate; rate-limited 10/min). Client-side confirm-match.
- [x] **Change email** — `POST /api/settings/email` (password-gated) creates a single-use, 1-hour `EmailChangeRequest` (SHA-256 token hash, migration `20260825120000_add_email_change_request`) and emails a `/verify-email?token=…` link to the **new** address via PI-29. Public `POST /api/auth/verify-email-change` applies it (re-checks uniqueness, marks used). New `VerifyEmailPage` route. Returns 503 if SMTP unconfigured (clear UI message).
- [x] **Delete account** — `POST /api/settings/delete-account`: verify password AND type the exact org name, then delete the organization (cascades to organizers/players/tournaments/pods/cards). Clears the session; client clears cache and redirects to `/login`. Revised further under PI-34 for multi-organizer orgs.
- [x] Browser + live-email verification done. (Note: the email form shows even when SMTP is unconfigured and surfaces the 503 message on submit — could hide it proactively by exposing an `emailConfigured` flag if wanted.)

### PI-29 — Transactional email via SMTP ✅
Prerequisite for PI-28's email-change verification (and any future notifications).
- [x] `nodemailer` transport built lazily from env config (`config.smtp`: host/port/user/pass/from/secure) + `APP_BASE_URL`. `services/mailer.ts`: `sendMail`, `isEmailConfigured`, `resolveBaseUrl`. Env documented in `.env.example` + passed through `docker-compose.yml`.
- [x] **Optional/degrades cleanly:** if `SMTP_HOST`/`SMTP_FROM` unset, `isEmailConfigured()` is false and `sendMail` throws `email_not_configured` — the app runs fine, email-dependent features (PI-28 email change) just aren't offered.
- [x] **Anti-spam posture:** the mailer is only wired to first-party account flows (PI-28 sends to the organizer's own new address; PI-34 co-organizer invites send to an address the inviting organizer supplies, which is the one deliberate exception — an org owner adding someone to their own org). Single-use + expiring verification tokens live in the PI-28/PI-34 flows.
- [x] Live verification (actually sending) done.

### PI-30 — Rename "Gesamtwertung" → "Tournament Standings" ✅
The tournament-wide standings feature/page uses a German term; rename it to **"Tournament Standings"** for a broader audience (deliberately *not* "Weekend Standings" — not every tournament spans a weekend).
- [x] **Scope decision: user-facing text only.** Renamed the three rendered strings (`GesamtwertungPage` title + "Weekend overview"→"Tournament overview" fallback, `PublicTournamentPage` heading, `TournamentPage` "View standings" link) plus the README's user-facing mentions (feature list + screenshot alt).
- [x] **Deliberately NOT renamed:** code identifiers (`GesamtwertungList`, `useGesamtwertung`, `computeGesamtwertung`, types), API endpoint paths, JSON response keys, query keys, the `/gesamtwertung` client route, and the historical German term in PLAN.md/BUILD-LOG.md (it's the real term the group used). The German word only hurts where a broad audience *reads* it (the UI); renaming endpoints/hooks is a cross-package (client/server/mcp/tests) API-contract ripple with zero user value and no runtime verification available here. A code-level rename is a separate mechanical pass if ever wanted.

### PI-31 — Richer Markdown for tournament descriptions ✅
The tournament description (built in the build log's PI-8 as a safe minimal renderer — plain text + auto-linked URLs + `[label](url)` only) should support fuller Markdown so detailed descriptions can be crafted. **No file uploads.**
- [x] `RichText` now renders full Markdown via `react-markdown` + `remark-gfm`: headings, ordered/unordered lists, **bold**/*italic*/<u>underline</u>, tables, code, blockquotes, strikethrough, links. Element styling centralized in a `.markdown` CSS scope (`index.css`) since Tailwind preflight strips defaults.
- [x] **XSS-safe:** react-markdown emits React nodes (no `dangerouslySetInnerHTML` for the Markdown). `rehype-raw` lets literal tags through only so `<u>` works, but `rehype-sanitize` runs after it with a strict allowlist that **drops `img`** (no image/file-embed surface) and adds only `u`. Links forced to `target=_blank rel=noopener noreferrer`.
- [x] Editor stays plain Markdown source (no live preview) with a "Markdown supported" hint on both the create form (`DashboardPage`) and the inline edit-in-place (`TournamentPage`). Renders on `TournamentPage` (authed) + `PublicTournamentPage`. Server description cap raised 4000→10000 for detailed content.
- [ ] Nice-to-have not built: a live side-by-side preview while editing.

### PI-32 — Secondary link color for Edit buttons (muted blue) ✅
Introduce a secondary link/action color, distinct from the accent — mainly for the "Edit" affordances (description, pod, tournament). A muted blue is the proposed direction.
- [x] Added `--color-link` (#7c9cbb, muted steel blue) + `--color-link-strong` (#9bb6d0, hover) to `index.css`'s `@theme`. Contrast checked via Node, not eyeballed: link vs bg 6.52 / surface 6.06 / raised 5.52; strong 8.89 / 8.26 / 7.53 — all clear 4.5:1 with headroom.
- [x] Applied `text-link hover:text-link-strong` to the Edit affordances: description edit + edit-tournament (`TournamentPage`), edit-pod (`PodPage`), and the reported-score Edit link (`PairingsPage`).

### PI-33 — Standalone pre-round timer (draft / deck-building) ✅
The round timer today only exists once a round is paired (it counts down from `Round.endsAt`). But a pod needs a timer *before* any round — for draft + deck-building time. Add a timer an organizer can start on a pod without pairing a round.
- [x] Schema: `Pod.prepTimerEndsAt DateTime?` + `Pod.prepTimerLabel String?` (migration `20260825100000_add_pod_prep_timer`, additive nullable columns). Independent of rounds — works with zero rounds paired.
- [x] Backend: `POST /api/pods/:id/prep-timer` (`{minutes (default 50), label?}`) and `DELETE .../prep-timer`, org-scoped; both broadcast `prep-timer-updated` on the pod room. `usePodRealtime` now invalidates on that event.
- [x] Frontend: reuses `useCountdown` (ticks client-side off `prepTimerEndsAt`). `PrepTimer` control (start length + optional label + Stop) on `PodPage`; read-only `PrepTimerDisplay` on `PublicPodPage` (large) and `PairingsPage` (enlarges in Display Mode). Live across devices via the existing socket.
- [x] Browser-verified. No chime wired (round-timer-only, per scope); MCP tools not added (organizer UI action, not a bulk op).

### PI-34 — Multiple organizers per org + explicit "delete organization" ✅
Split out from the PI-28 account-deletion decision (2026-08-25). The data model already allows multiple `OrganizerAccount`s per org, but there's no way to add one, and no standalone org-delete.
- [x] **Invite/add co-organizers** to an organization. New `OrganizerInvite` model (migration `20260825130000_add_organizer_invite`) — single-use hashed token, 7-day expiry, same pattern as PI-28's `EmailChangeRequest`. `GET/POST /api/settings/organizers[/invite]`, `DELETE .../invites/:id`, `DELETE .../organizers/:id` (session-auth only); public `GET /api/auth/invite/:token` + `POST /api/auth/accept-invite` (invitee sets their own name/password, logged in immediately). Requires SMTP (PI-29) — no other way to deliver the link. Roles equal for v1: an accepted invite is a full `OrganizerAccount`, same access as anyone else. Frontend: new "Organizers" section in Settings (invite form, organizer list, pending-invite list) + `AcceptInvitePage` at `/accept-invite`.
- [x] **Explicit "delete organization"** action (`POST /api/settings/delete-organization`, hard-gated: password + type org name) — always deletes the whole org regardless of organizer count. Distinct from "delete my account". Only shown in Settings when `organizerCount > 1` (for a solo organizer, "Delete account" already does this — a second identical-looking danger button would just be clutter).
- [x] Revisited PI-28's cascade: `POST /api/settings/delete-account` now checks `organizerCount` — if >1, it only removes the caller's own `OrganizerAccount` ("leave organization", confirmed by typing your own email) and leaves the org untouched; if it's the last organizer, unchanged full-org-delete behavior (confirmed by typing the org name). `organizerCount` added to `/auth/login` and `/auth/me` responses so the frontend knows which copy/behavior to show.

### PI-35 — Footer bar with legal links and GitHub link ✅
Add a site-wide footer bar: legal links (e.g. Impressum/Privacy — decide what's actually needed for a self-hosted OSS app vs. what individual deployers should supply) and a link to the GitHub repo.
- [x] **Scope decided:** GitHub + License links ship built-in (no legal content of our own). A third link is a deployer-configured slot: `LEGAL_LINK_URL` + `LEGAL_LINK_LABEL` env vars, surfaced via a new public `GET /api/app-config` (`server/src/routes/auth.ts`, config in `server/src/config.ts`), rendered only when both are set. Documented in `.env.example` + passed through `docker-compose.yml`.
- [x] **Placement:** new shared `Footer.tsx` (`client/src/components/Footer.tsx`, backed by `useAppConfig` in `client/src/features/config/useAppConfig.ts`), rendered in both `Layout.tsx` (authed) and `PublicLayout.tsx` (public `/o/:slug/...`), after `<main>` inside the shared `min-h-screen` chrome — same "shared chrome" pattern `TopBar` already uses.
- [x] GitHub link target: `https://github.com/TobiasDax/LimitedGauntlet` (License links to `/blob/main/LICENSE` on the same repo).

### PI-36 — Constructed format dropdown (Standard, Modern, Legacy, etc.) ✅
Pods with `format: CONSTRUCTED` currently don't record *which* constructed format was played. Add a dropdown for that: Standard, Modern, Legacy, Vintage, Pioneer, Pre-Modern, Pauper, plus a **Custom** option that reveals a free-text field to record an arbitrary format name.
- [x] New `ConstructedFormat` enum + `Pod.constructedFormat`/`Pod.constructedFormatCustom String?` fields (migration `20260825140000_add_pod_constructed_format`), mirroring the `setCode` optional-field pattern. Both optional; application-layer enforced (not a DB constraint) that `constructedFormat` only applies to `CONSTRUCTED` pods and `constructedFormatCustom` only pairs with the `CUSTOM` option — see `constructedFormatError()` in `server/src/routes/pods.ts`.
- [x] **Optional, not required** — a constructed pod can leave it unset. Displayed via a new `podFormatDisplay()` helper (`client/src/features/pods/usePods.ts`) as "Constructed — Modern" etc., wherever the plain format label used to show (pod header, tournament pod list, public pod/tournament pages, standings, value page).
- [x] New shared `ConstructedFormatPicker` component (`client/src/components/ConstructedFormatPicker.tsx`), wired into both `NewPodForm` (`TournamentPage.tsx`) and `EditPodForm` (`PodPage.tsx`) — shown only when `format === "CONSTRUCTED"`, same conditional pattern as the existing `SetPicker`.
- [x] Browser-verified (2026-09-02).

### PI-37a — 2v2 Constructed ✅
Support the 2v2 multiplayer constructed pod type beyond 1v1. (4-player Commander was considered alongside this but dropped as out of scope for the app — 1v1/team Swiss is the model this app commits to.)
- [x] **2v2 needed zero pairing/standings changes** — confirmed the existing team-pod machinery (`Entrant` unifying individuals/teams) is already format-agnostic: no `format`-conditional branching anywhere in `pairing.ts`/`standings.ts`/`podStats.ts`/`weekendHistory.ts`. A `CONSTRUCTED` pod with `isTeamEvent: true, teamSize: 2` already pairs/scores correctly today.
- [x] **Found and fixed the one real gap:** nothing previously validated that a team-entrant's `playerIds.length` matched `pod.teamSize` — an organizer could add a "2-player team" with 1, 3, or 5 members and nothing rejected it. Fixed server-side (`POST /api/pods/:id/entrants`, `pods.ts`) with a `wrong_team_size` 400, plus a matching client-side guard + "x / N selected" hint in `TeamEntrants` (`PodPage.tsx`) that disables submit until the count matches.
- [x] Browser-verified (2026-09-02).

### PI-38 — Organizer data export ✅
Give an organizer a way to export all their org's data in a machine-readable form, so it can be moved/backed up or imported elsewhere (pairs with PI-39's import).
- [x] **Contents:** structural `data` (players, tournaments, pods, teams, entrants, rounds, matches, card pulls) — a faithful, round-trippable dump keyed by in-pod refs (player displayName / team name), the same identity PI-39 re-resolves — plus optional **Hall of Fame** and **Treasure Vault** snapshots (derived, informational). New `server/src/services/orgExport.ts` + `GET /api/settings/export?data=&hallOfFame=&treasureVault=` (session-auth only, never bearer).
- [x] **UI:** "Export data…" button in a new **Export / Import** Settings section opens a popup (`ExportImportSection.tsx` + shared `Modal` in `ui.tsx`) with a checkbox per section; downloads `<slug>-export-<date>.json` via `useExportOrg` (direct fetch → Blob, not the JSON `api` client).
- [x] Lives in the shared **Export / Import** Settings section (with PI-39).

### PI-40 — History import: default slug + duplicate-org handling ✅
Cleanups to the existing `import-legacy.ts` flow (`server/src/scripts/import-legacy.ts`).
- [x] **Default slug:** `upsertOrg()` now defaults `IMPORT_ORG_SLUG` to `gp` and `IMPORT_ORG_NAME` to `GP` (was `gp-eichstaett` / `GP Eichstätt` — too author-specific). README + `import-history` SKILL.md updated.
- [x] **Duplicate-org handling:** the tool checks for an existing org **by slug** (`findUnique({ where: { slug } })`) and reuses it (tournament/pod idempotency by name prevents dupes on re-run). Added a **name-collision guard**: since org name isn't unique, importing under a *different* slug when an org with the same *name* already exists now throws with a clear message (re-run with the existing slug, or set `IMPORT_ALLOW_DUPLICATE_NAME=1` to deliberately create a separate org). Closes the silent-duplicate footgun before PI-39 exposes import via the UI.

### PI-41 — Roadmap overview in the README ✅
Surface a short roadmap snapshot in `README.md` for quick visibility, so a visitor/self-hoster can see project status and what's planned without opening `ROADMAP.md`.
- [x] Added a brief "Roadmap" section to the README (between Development and History import): status + latest-release link + a short bullet list of notable backlog items (export/import, OIDC, share/QR, recent polish), linking to `ROADMAP.md` for full detail.
- [x] Kept it **short and pointer-style** — the canonical list stays here in `ROADMAP.md`; the README is a teaser (doesn't duplicate every PI item), so the two don't drift.

### PI-42 — Register / Login via OIDC ✅
Add OIDC as a login/registration option so the app can be used with an external identity provider (self-hosted Authelia/Keycloak/etc.), alongside the existing email+password organizer accounts.
- [x] Standard OIDC **authorization-code + PKCE** flow via `openid-client` v5 (`server/src/services/oidc.ts`, memoized discovery). Configured per-deployment via env (`OIDC_ISSUER`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET`, optional `OIDC_REDIRECT_URI`/`OIDC_PROVIDER_NAME`/`OIDC_SCOPE`), `isOidcConfigured()`; degrades cleanly to password-only when unset (same posture as SMTP in PI-29 — button hidden). Routes: `GET /api/auth/oidc/login` (redirect, stashes state/nonce/verifier in the session) + `GET /api/auth/oidc/callback`. Env wired into `.env.example` + both compose files; documented in `docs/deployment.md` §8.
- [x] **Account linking by verified email:** callback resolves identity in order — known `oidcSubject` → existing account by verified email (records the subject) → pending co-organizer invite for that email (provisions a passwordless account into that org, PI-34 reuse) → else refused (`oidc_no_account`). **Never creates a new org**, preserving the closed-signup posture. Session model unchanged (`@fastify/secure-session` cookie — OIDC only establishes identity, no external store). Schema: `OrganizerAccount.passwordHash` now nullable + `oidcSubject String? @unique` (migration `20260826120000_add_organizer_oidc`); password login rejects passwordless accounts; SSO-only accounts can set a first password in Settings. **Live-verified 2026-08-26** against Pocket ID on the DaxLite deployment (tsdproxy hostname, `APP_BASE_URL`/redirect URI configured per `docs/deployment.md` §8) — login via existing organizer account confirmed working end-to-end.
- [x] **Org/first-run semantics (revised per Tobias 2026-08-26):** SSO **self-registration is supported** when `ALLOW_SIGNUP=true` — an unknown verified identity is stashed in the session and sent to a one-time **org-setup screen** (`OidcSetupPage` at `/oidc-setup`; `GET /api/auth/oidc/pending` + `POST /api/auth/oidc/complete-registration` create the org + passwordless organizer). When signups are closed, unknown identities are still refused (`oidc_no_account`), so join-by-invite/existing-only remains the closed-signup behavior. Frontend: "Sign in with <provider>" button on `LoginPage` (via `useAppConfig`) + `?error=` message mapping.
- [x] **SSO-only mode:** `LOCAL_LOGIN_DISABLED=true` (env) disables local password login + local signup so OIDC is the only way in — `POST /api/auth/login`/`/signup` return `local_login_disabled`, and `LoginPage`/`SignupPage` hide the password form + signup link (surfacing only the SSO button). Fail-safe: `isLocalLoginDisabled()` is only honoured when OIDC is actually configured, so the flag can't lock everyone out. Wired into `.env.example`, both compose files, deployment.md §8.
- [x] **Follow-up shipped (2026-09-05), scope grew from a UI tweak to a bug fix:** an SSO-only account wasn't just missing the "Set password" affordance — it was *locked out* of Settings → Account entirely, since email change / delete account / delete organization all hard-required a current password it doesn't have (returned `password_required`). Fix: `/auth/me` now returns `hasPassword` (from `request.organizer.hasPassword`, set in `middleware.ts`'s `setOrganizer` off `passwordHash != null`). Settings → Account branches on it — "Change password" becomes "Set password" (no current field), and the three destructive forms drop the password field, re-verifying via the typed org-name/email confirmation alone (decided with Tobias 2026-09-05: consistent with the rest of the app, where no other destructive action is password-gated; forcing an SSO user to create a password just to leave was the alternative and was rejected). `settings.ts`'s three routes now only verify a password when the account has one, matching what `POST /api/settings/password` already did. Server suite 136/136; shipped in v0.5.3, browser-verified.

### PI-43 — Social login: Google + Discord ✅
Google and Discord as social-login options on top of PI-42's account-linking machinery. (Discord added to the same pass at Tobias's request.)
- [x] **Provider registry** (`server/src/services/sso.ts`, replacing `services/oidc.ts`): three providers, each independently optional (same "degrades to password-only" posture as SMTP). `oidc` (existing generic OIDC, unchanged) and `google` share the `openid-client` flow — Google is just the fixed issuer `https://accounts.google.com`. **Discord is not OIDC** (no `id_token`), so it gets a hand-rolled OAuth2 + PKCE path: authorize → token exchange → `GET /users/@me`, identity from `{ id, email, verified, global_name/username }`. No new dependency.
- [x] **One SSO identity per account** (decision: Tobias). `OrganizerAccount.oidcSubject` now stores a **provider-prefixed** key (`google:123` / `discord:456` / `oidc:<sub>`) — no new table. Data-only migration `20260902160000_prefix_sso_subject` backfills existing generic-OIDC subjects (+ any live relink requests) with the `oidc:` prefix; guard clause makes it re-run-safe. Signing in with a *second* provider for an already-linked account routes through the **existing PI-49 relink flow** (mailbox link / operator CLI), never a silent rebind.
- [x] **Routes:** `GET /api/auth/sso/:provider/{login,callback}` (`:provider` ∈ oidc|google|discord). `/api/auth/oidc/{login,callback}` kept as back-compat aliases so the already-registered Pocket ID redirect URI needs no change. Session key `sso: { provider, … }` (old `oidc` key still read for one release). `linkOrProvisionFromSso` (moved into `sso.ts`, was `linkOrProvisionFromOidc` in `auth.ts`) — logic unchanged beyond the prefix.
- [x] **Config:** `GOOGLE_CLIENT_ID/SECRET`, `DISCORD_CLIENT_ID/SECRET` (`.env.example` + both compose files). `configuredSsoProviders()` drives the login UI; `isLocalLoginDisabled()` now gates on *any* SSO provider being configured, not just OIDC.
- [x] **Frontend:** `GET /api/app-config` returns `ssoProviders: [{ id, label }]`; new shared `SsoButtons` component renders one button per provider on `LoginPage` + `SignupPage` (SSO-only mode). Error handling / `?error=` codes unchanged.
- [x] **Tests:** `server/src/services/sso.test.ts` — Discord `/users/@me` parsing (verified/unverified/missing email, `global_name` fallback), `configuredSsoProviders()` combinations, and `linkOrProvisionFromSso` against real Postgres (prefixed-subject login, verified-email link records the prefix, second-provider → `recovery_required`, unverified refusal, closed-signup refusal). New `server/vitest.config.ts` supplies a throwaway `SESSION_SECRET` for tests that transitively load `config.ts`.
- [x] Browser-verified with real Google + Discord OAuth apps.

### PI-44 — Public demo instance ✅ (deployed and live 2026-09-02)
A public demo deployment so prospective self-hosters can try the app without standing one up.
- [x] **Decided (Tobias, 2026-08-31):** a fully separate deployment (own Postgres, own throwaway data) on an existing-but-unused VPS, hosted **outside this repo** — the compose file/reset script/seed fixture are personal infra, not something other self-hosters need, so they live in a standalone project at `~/Projects/limitedgauntlet-demo/` instead of here.
- [x] **Reset mechanism:** no pg_dump/volume-snapshot tooling needed — the app already has a clean "load this data" primitive (PI-38/39's export/import), so reset = wipe the Postgres volume, bring up a fresh instance (migrations run automatically on start, same as any deploy), sign up the fixed demo login, then `POST` an anonymized seed export to `/api/settings/import`. Runs hourly via cron.
- [x] **Anonymization:** since the export format (`orgExport.ts`) uses each player's `displayName` as the identity key throughout the whole document, a plain exact-string swap — real name → fictional name, everywhere it appears — preserves every relationship in the file while removing every real name. A small script does this against a real export pulled from Settings → Export/Import.
- [x] **Known demo login:** a fixed, published organizer credential, same idea as originally scoped.
- [x] **Deployed and live 2026-09-02** at [limited-gauntlet.com](https://limited-gauntlet.com); linked from the README. Full detail (compose file, reset script, anonymizer, setup steps) lives in `~/Projects/limitedgauntlet-demo/README.md` and the `claude-memory` infra docs, not here.

### PI-45 — Share popup with QR code for a pod's public link ✅
The pod page's "Public link ↗" (`PodPage.tsx`) was a plain anchor that just opened the public pod page in a new tab. Turned it into a proper share affordance: a popup showing the shareable link plus a generated QR code so players can scan it to open the public page on their phone.
- [x] "Share public link ↗" opens a modal (`SharePopup.tsx`, on the shared `Modal` in `ui.tsx`) with the full public URL (read-only field + Copy button, with an execCommand fallback for non-HTTPS LAN where the clipboard API is unavailable) and a **QR code** encoding it.
- [x] QR generated client-side via `qrcode.react` (`QRCodeSVG`) — pure-JS, no server round-trip, works offline once loaded. URL built from `window.location.origin` so it matches whatever host the organizer is on.
- [x] **Generalized to every public surface** (it composed cleanly): pod (primary), tournament (`TournamentPage`), org home (`DashboardPage`), Hall of Fame (`HallOfFamePage`), Treasure Chest (`TreasureChestPage`) — each old "Public link ↗" anchor is now a Share button opening the popup.

### PI-46 — Security: authorize Socket.IO subscriptions for locked public data ✅
Codex Security finding `csf_09568eb85fba75844b9037e5` (medium, high confidence; CWE-862) found that `server/src/realtime.ts` accepts any `pod:<id>` or `tournament:<id>` room name by prefix. A visitor who still knows a shared resource ID can therefore subscribe without satisfying PI-27's organization password lock and receive future full pairing/result payloads emitted by `server/src/routes/rounds.ts`.
- [x] **Authorization mechanism:** `createRealtimeRoomAuthorizer` (`server/src/realtime.ts`) parses the existing secure-session cookie during the Socket.IO `join` handshake — no separate capability token needed.
- [x] Every `pod:<id>`/`tournament:<id>` join resolves the room to its organization first. Access is permitted only when the org has no public lock, the session's `publicUnlocked` list contains it, or the session's `organizerId` belongs to that org; malformed rooms, nonexistent resources, and locked-without-session all fail closed (`ack({ ok: false })`) without a DB lookup leaking which case applied.
- [x] `emitPodEvent`/`emitTournamentEvent` now discard their payload argument and only ever broadcast `{ podId }`/`{ tournamentId }` — callers in `rounds.ts` still pass rich payloads (`{ match: updated }`, `{ round }`) but those are now dead arguments; clients refetch via the authorized HTTP API. Lock-status changes call `refreshRealtimeAuthorization()` to force a reconnect so joins re-evaluate against fresh state.
- [x] `server/src/realtime.test.ts` covers unlocked access, locked-without-session denial, unlock-via-`publicUnlocked`, same-org vs. cross-org organizer access, malformed/nonexistent rooms, and re-evaluation of a lock flip across two joins — plus an end-to-end Socket.IO test asserting a denied join never joins the room and an allowed one receives only the minimal payload.
- [x] **Acceptance verified:** a locked-out socket cannot join a locked room at all (no events of any kind reach it); unlocked/organizer joins still work; the HTTP public-lock behavior (`public.ts`'s preHandler) is untouched.
- [x] Re-verified against `server/src/realtime.ts`, `server/src/routes/rounds.ts` (all `emitPodEvent`/`emitTournamentEvent` call sites), and `server/src/routes/public.ts`'s lock preHandler as part of the v0.2.0 release audit.

### PI-47 — Security: bound and transactionalize organization imports ✅
Codex Security finding `csf_86d99714c3f7c9b0b2c21419` (medium, high confidence; CWE-400) found that PI-39's 25 MiB import accepts unbounded nested arrays and performs sequential per-record Prisma writes without rollback. One organizer session can amplify a compact upload into excessive shared database work and durable storage.
- [x] `IMPORT_LIMITS` (`server/src/services/orgImport.ts`) sets explicit per-field/per-collection maximums (players, tournaments, pods, teams/members, entrants, rounds, matches, card pulls, every stored string) and every schema is `.strict()` so unknown fields are rejected outright rather than silently ignored.
- [x] `measureImport()` computes an aggregate record count + total string-character budget in `parseOrgExport` and rejects the whole document (`import_too_large`, mapped to HTTP 413) before any database work starts.
- [x] `importOrgData` wraps the entire import in one `prisma.$transaction` (`maxWait: 5s`, `timeout: 120s`) via `importOrgDataInTransaction` — any failure rolls back the whole file, matching the "complete file, not per-tournament" choice.
- [x] An in-process `importInProgress` flag throws `ImportInProgressError` (mapped to HTTP 409) on a concurrent attempt for the same server instance — documented as the concurrency control rather than per-row batching (writes stayed per-row for referential clarity, not `createMany`, since imports are bounded and infrequent).
- [x] `server/src/services/orgImport.test.ts` covers a valid small export, malformed dates rejected pre-write, a collection over its limit, unknown/padding fields rejected, and an aggregate string-budget breach — narrower than every named boundary (no explicit concurrent-attempt or rollback-on-late-failure test), but the transaction wrapper makes rollback structural rather than something that needs its own regression test.
- [x] **Acceptance verified:** over-budget imports fail before any write (schema/budget checks run entirely in `parseOrgExport`); the transaction makes a failed accepted import leave no partial rows; the `importInProgress` guard stops concurrent imports; PI-38 exports still round-trip (existing export/import pairing unchanged).
- [x] Re-verified against `server/src/services/orgImport.ts` and the `/api/settings/import` route (`server/src/routes/settings.ts`) as part of the v0.2.0 release audit.

### PI-48 — Security: make proxy trust and rate-limit identity deployment-safe ✅
Codex Security finding `csf_15c64d317da4d05b9e578f79` (medium, high confidence; CWE-307) found that global `trustProxy: true` lets a client reaching the app directly rotate `X-Forwarded-For` values and obtain fresh authentication rate-limit buckets. This conflicts with the Compose files publishing the listener directly and the documented LAN deployment option.
- [x] **Deployment intent (Tobias, 2026-08-26, revised 2026-08-26):** the shipped/default Compose files bundle no reverse proxy or tunnel and publish no host port — no ingress at all by default. A deployer adds exactly one of: a published host port (LAN/direct), or their own reverse proxy/tunnel (Cloudflare Tunnel, Nginx, Caddy, Traefik, ...) — possibly running in a separate Compose project, as Tobias's own Cloudflare Tunnel does — connected over a private Docker network, via a `docker-compose.override.yml`. Whichever is chosen, `TRUSTED_PROXIES` is set to match (blank for direct, the proxy's exact container IP/CIDR otherwise). See `docs/deployment.md` section 4.
- [x] `trustProxy: true` replaced with `trustProxy: config.trustedProxies` (`server/src/index.ts`), fed by `parseTrustedProxies()` (`server/src/proxyTrust.ts`) — accepts only exact IPs/bounded CIDRs, throws clearly on `true`/`*`/unbounded ranges/malformed entries, defaults to `false` (no trust) when unset.
- [x] No extra header-overwrite code needed: Fastify's `trustProxy` mechanism (via `proxy-addr`) already derives `request.ip` from the raw socket address whenever the immediate peer isn't in the trusted list, ignoring `X-Forwarded-*` entirely in that case — verified by `proxyTrust.test.ts`.
- [x] Direct/LAN deployments are the explicit default now (no bundled proxy, no published port, blank `TRUSTED_PROXIES`) — self-hosters opt into a proxy rather than inheriting Tobias's topology.
- [x] Documented recommended examples for direct LAN and a reverse proxy/tunnel (including one running in its own separate Compose project) in `.env.example`, both Compose files, and `docs/deployment.md` section 4, including Docker networking, port exposure, trusted peers/hops, and required header-overwrite behavior.
- [x] `server/src/proxyTrust.test.ts` covers: rotated forwarding headers ignored for a direct/untrusted client (still rate-limited on the real socket address), a trusted peer's forwarded IP used verbatim, an untrusted peer's forwarded headers ignored even when a trust list is configured, and the rightmost-untrusted-hop rule in a multi-entry forwarded chain.
- [x] **Acceptance verified:** rate-limit identity can't be rotated by a direct client regardless of headers sent; a configured trusted proxy's forwarded IP is honored; an unconfigured/unknown peer never influences it; existing login/signup/public-unlock rate limits are untouched by this change (they key off the same `request.ip`).
- [x] Re-verified against `server/src/index.ts`, `server/src/routes/auth.ts`'s rate-limited routes, and the Compose port mappings as part of the v0.2.0 release audit.

### PI-49 — Security: reject conflicting OIDC subject bindings ✅
Codex Security finding `csf_75d7f7bccdd1a727f883f142` (medium, medium confidence; CWE-287) found that PI-42's verified-email fallback authenticates an existing organizer even when that account is already bound to a different `oidcSubject`. If the provider reassigns or duplicates a verified email, the new subject could receive the existing organizer session.
- [x] **Identity intent (Tobias, 2026-08-26):** Pocket ID is the intended provider. A Pocket ID account may be deleted and recreated with the same mailbox but a different subject. Existing subject bindings remain authoritative for normal login, but possession of the existing email mailbox is accepted as the recovery authority for an explicit relink.
- [x] `linkOrProvisionFromOidc` (`server/src/routes/auth.ts`) allows email-based first linking only when the matching account has no `oidcSubject` (an atomic `updateMany({ where: { oidcSubject: null } })`, so a race can't double-link). A non-null differing subject never creates a session — it returns `recovery_required` and logs a safe event (organizerId + requestId + whether an email was sent; no claims/secrets).
- [x] Known-subject path unchanged and authoritative: `completeOidcLogin` (`server/src/services/oidc.ts`) still verifies state/nonce/PKCE via `openid-client`'s `client.callback()` and only trusts `email_verified === true`; `oidcSubject` stays `@unique` in the schema.
- [x] Explicit subject-relink recovery flow (`server/src/services/oidcRelink.ts`): `createOidcRelinkRequest` mints a single-use, 1-hour, SHA-256-hashed token bound to the organizer, pending subject, email, and purpose (`OidcSubjectRelinkRequest`, migration `20260826122000_add_oidc_relink`); `confirmOidcRelink(token)` validates purpose/expiry/email-still-matches inside a transaction before applying anything.
- [x] On confirm, `applyOidcRelink` (shared by both confirmation paths) replaces `oidcSubject`, increments the account's `authVersion` (invalidating every existing session — enforced in `auth/middleware.ts`'s `requireAuth`/`requireSessionAuth` **and now in `realtime.ts`'s room authorizer**, so an already-open realtime subscription is cut off too), deletes all `ApiToken` rows for the organizer, and consumes any other outstanding relink requests. `POST /api/auth/oidc/relink` (public, token-gated, rate-limited) returns a generic `{ok:true}`/`invalid_or_expired` — never discloses account existence.
- [x] **Recovery confirmation UI:** `OidcRelinkPage` (`client/src/routes/OidcRelinkPage.tsx`, route `/oidc-relink`) — public, fires the confirm once on mount from the emailed link's `?token=`, shows a generic done/error state. `LoginPage` also maps `oidc_recovery_required` (email sent) vs. `oidc_recovery_required_no_email` (SMTP unconfigured, points at the CLI below) to distinct messages.
- [x] **Operator CLI** (`server/src/scripts/oidc-relink.ts`, `docker compose exec app node server/dist/scripts/oidc-relink.js <email> [--yes]`) for SMTP-less deployments: requires direct host/operator access (no HTTP route), resolves the pending request via `findPendingOidcRelink(email)` — so the subject it applies always originated from a real, already-verified OIDC login attempt, never operator input — previews the exact current-vs-pending change, and requires typing `yes` (or `--yes` for scripted use) before calling `confirmOidcRelinkByRequestId`. Manually verified end-to-end against a live Postgres: preview, decline (no-op), confirm (subject rotated, `authVersion` bumped, API token deleted), and all three error paths (unknown email, no pending request, missing args).
- [x] Tests: `server/src/services/oidcRelink.test.ts` (6 tests — mailbox-token confirm incl. side effects, unknown/expired/replayed token rejection, email-changed-since-request rejection, `findPendingOidcRelink` for known/unknown/no-pending, operator-CLI confirm-by-id and its rejection of an unknown/reused id) and `server/src/realtime.test.ts`'s added authVersion-mismatch case. Full suite (43 tests across 8 files) run against a real Postgres, all green; server + client `tsc -b` clean.
- [x] **Acceptance verified:** a different subject can never authenticate as an already-bound organizer through email equality alone (returns `recovery_required`, no session); mailbox-confirmed and operator-confirmed recovery both deliberately replace the binding (tested both paths); the known-subject login and first-link-when-unbound flows are untouched and still work.
- [x] Re-verified against `server/src/routes/auth.ts` and `server/src/services/oidc.ts` as part of implementing this item.

### PI-50 — Outbound webhooks for round-timer/pairing events ✅
Originally framed as an MQTT integration for Home Assistant specifically (announce round events, drive a live round-timer display — concretely, Tobias's AWTRIX matrix clock: remaining time + a chime). Revised 2026-08-26 to **plain HTTP webhooks** instead: simpler to build (no persistent broker connection, no stored broker credentials, no client library), and more flexible for other self-hosters — any system that accepts an HTTP POST (Home Assistant, Node-RED, n8n, a Discord relay, etc.) can consume it, not just MQTT-speaking setups. One-way only for v1 (LimitedGauntlet → webhook URL); no inbound control.
- [x] **Mechanism:** `POST` with an `X-LimitedGauntlet-Signature: sha256=<hmac>` header (HMAC-SHA256 of the raw JSON body) to the organizer-configured URL (`server/src/services/webhooks.ts`). The receiving side derives the live countdown itself from the `endsAt` timestamp each payload carries — same approach the app's own `useCountdown` hook uses — so this never streams "remaining time" continuously, just fires discrete lifecycle events. A chime is purely a receiver-side automation decision triggered off that derived countdown.
- [x] **Config scope:** per-organization, in Settings → a "Webhooks" section (`client/src/components/settings/WebhookSection.tsx`) — an org can configure **any number** of webhooks (revised 2026-08-26 per Tobias, from an initial single-URL design), each with its own URL, viewable/copyable signing secret (independently regenerable), optional label, and "Send test event" button; delivered to independently and in parallel, so one slow/broken receiver never delays or blocks the others. `OrganizationWebhook` model (migration `20260826150000_webhooks_multi`, replacing the original single `Organization.webhookUrl`/`webhookSecret` columns from migration `20260826140000_add_webhooks` — the new migration backfills any existing single config into the new table before dropping those columns, so nothing already configured is lost). Zero rows = inert, same posture as SMTP/OIDC. Secrets are stored as plaintext, not hashed — deliberately, since this app is the one *presenting* them (as the HMAC key), not verifying something a caller presents.
- [x] **Publish scope:** default **on** for every pod; `Pod.webhookEnabled` (default `true`) lets an organizer opt a specific pod out from its edit form, mirroring `excludeFromStats`.
- [x] **Event set:** `round.started`, `round.extended`, `round.completed`, `pairings.posted` — fired from `server/src/routes/rounds.ts` alongside the existing realtime broadcasts, non-blocking (`fireAndForget`: the DB reads to build a payload and the HTTP POST both happen after the triggering request can already respond).
- [x] **`pod.completed` (added 2026-08-26 per Tobias — display the winner for 5 min with a celebratory icon):** fires alongside `round.completed` specifically when that round was the pod's last configured round (`round.roundNumber >= pod.roundCount`) — there's no separate "mark pod complete" action in the app, so this is the natural completion moment. Reuses the same standings computed for `round.completed` (no extra query); payload adds `winner` (the rank-1 standings entry, or `null`) and `roundNumber`. `round.completed` itself also gained an `isLastRound` boolean (same underlying flag) so a receiver only listening to that one event doesn't need to also handle `pod.completed`. Live-verified: completing a 2-round pod's round 1 fires only `round.completed` (`isLastRound: false`); completing round 2 fires both `round.completed` (`isLastRound: true`) and `pod.completed` with the correct winner.
- [x] **Rich payloads (revised 2026-08-26 per Tobias — payload size isn't a real constraint for an HTTP POST):** every event carries `podId`/`podName`/`tournamentId`/`tournamentName` context automatically. `pairings.posted` and `round.started` include the full match list with resolved entrant display names (`buildMatchesPayload`) — not just IDs — so a receiver can announce "Round 3: Alice vs. Bob at table 1" without calling back into the API. `round.completed` includes the updated, ranked standings (`buildStandingsPayload`, reusing `computePodStandings`) — "who's playing and their current standing" is in the payload, not something the receiver has to fetch separately.
- [x] **SSRF guard, scoped correctly:** `isSafeWebhookTarget` resolves the hostname and refuses loopback/link-local targets only (127.0.0.0/8, 169.254.0.0/16 incl. cloud metadata, `::1`, `fe80::/10`) — deliberately **not** the broader RFC1918 ranges, since the real-world target (a self-hoster's own Home Assistant on their LAN) is typically a 192.168.x/10.x/172.16-31.x address; blocking those would break the primary use case. Best-effort, not airtight (documented DNS-rebinding caveat in the code comment).
- [x] Delivery is fire-and-forget with a 5s request timeout (no retry queue — this app deliberately carries no background-job infrastructure); a failed delivery is logged and dropped, never surfaced to the organizer UI outside of the explicit "Send test event" button, which does report success/failure/HTTP status back.
- [x] Tests: `server/src/services/webhooks.test.ts` (18 tests) — address classification, the SSRF guard (including that it explicitly allows a LAN-shaped address and blocks loopback/link-local), HMAC signature correctness and non-2xx/connection-failure handling via a real local HTTP server, `sendWebhookEvent`'s not-configured/opted-out/SSRF-skip branches, fan-out to multiple webhooks with independent per-webhook signatures, resilience when one of several webhooks is unreachable, `sendTestWebhookEvent` targeting exactly one webhook by id, and `buildMatchesPayload`/`buildStandingsPayload` name resolution and ranking against real DB data. Full suite (61 tests across 9 files) run against a real Postgres container, including both migrations applied in sequence.
- [x] **End-to-end verified live** twice, not just unit-tested: once for the original single-webhook design and again after the multi-webhook revision — built the actual Docker image each time, ran it against a real Postgres with separate HTTP receiver containers, and drove the full HTTP API (signup → tournament → pod → entrants → generate pairings → start → extend → submit result → complete). Confirmed all four events arrive with correct signatures/payload contents; the per-pod opt-out suppresses events for that pod only; disable/re-enable/regenerate-secret/invalid-URL-rejection all behave correctly; and — for the multi-webhook revision — two independently configured webhooks both receive the identical event body with distinct correct signatures, "send test event" targets only the specified webhook (the other receiver gets nothing), and delete/regenerate-secret operate on exactly the targeted webhook without touching others.

### PI-51 — Draft seating chart (derived from round 1 pairings) ✅
Tobias flagged a gap: draft pods (the only format where seating around a physical table matters — packs get passed hand-to-hand) had no seating chart, even though it's fully derivable from round 1's pairings using the standard MTG cross-pairing convention.
- [x] **Formula (confirmed against Tobias's own worked example):** for an N-seat pod, M = ceil(N/2) is the table count; seat *i* pairs with seat *i+M* in round 1. An odd N's single bye always lands on seat M (the only table whose partner slot, M+M = N+1, falls outside the pod). Which entrant of a pair gets the lower vs. higher seat number is arbitrary — round 1's raw `tableNumber` values are arbitrary too (whichever order the pairing engine happened to build them in), so `computeSeatings` (`client/src/lib/seatings.ts`) sorts by existing `tableNumber` only for a stable, non-flickering render order and re-numbers 1..M itself.
- [x] **Derived, not stored:** computed client-side from round 1's `Match` rows each render — a manual pairing swap on round 1 (via the existing "click two names to swap seats" feature) corrects the seating chart automatically, no separate data model or migration needed.
- [x] **Scope (Tobias, 2026-08-26):** DRAFT and CHAOS_DRAFT pods only — Swiss/other formats don't have a physical seating concept. Shown inline at the top of the Pairings page (`client/src/routes/PairingsPage.tsx`), and only while round 1 is still `PENDING` — once round 1 starts, the physical draft has already happened and the page's focus shifts to gameplay, so the chart gets out of the way automatically (`showSeatingChart` gate).
- [x] **Visual design (Tobias, referencing the group's existing Outline layout):** a styled two-row grid (`client/src/components/SeatingChart.tsx`, not a plain table) — row 1 holds seats 1..M left-to-right, row 2 holds seats N..M+1 right-to-left, so reading row 1 then row 2 traces the seating order clockwise around the table. A bye's seat renders as a dashed/muted "Round 1 bye" cell. For an odd N, the one unused chair in this two-row layout is always rendered under seat 1 (a blank leading grid cell in row 2), not wherever the bye's table happens to fall — so every seat number keeps a fixed, predictable position on the imaginary table regardless of pod size or who drew the bye.
- [x] Verified via an ad-hoc Node script (no client test framework exists in this repo) covering 8-seat clean, 8-seat with out-of-order `tableNumber`s, 7-seat and 5-seat with a bye at a non-final raw table number, and the empty/no-round-1 case. Visually confirmed end-to-end via a full Docker build + Postgres + seeded 7-entrant DRAFT pod + Playwright screenshot, including the fixed-empty-slot-under-seat-1 layout.

### PI-52 — Player accounts for self-service check-in ✅
Idea from Tobias: let non-organizer users (players) have accounts so they can check themselves in / report their own game results, instead of everything going through an organizer.
- [x] **Identity model:** auth merged onto `Player` as nullable columns (`email`/`passwordHash`/`authVersion`, migration `20260902120000_add_player_accounts`) — same PI-33 "nullable columns for a 1:1 feature" precedent, and consistent with `OrganizerAccount` already merging person + auth. A plain roster entry has all three null. `Player` and `OrganizerAccount` stay separate rows for the same human: an organizer who also plays keeps entering results through the organizer UI and needs no player account; a player "promoted" to organizer just gets a fresh `OrganizerAccount` via the existing PI-34 invite, unrelated to their `Player` row.
- [x] **Second auth surface, never a wall:** new `playerId`/`playerAuthVersion` session keys (independent of `organizerId`), `requirePlayerAuth` middleware, `services/playerAccounts.ts` for the DB logic + thin `routes/playerAccounts.ts` HTTP wrapper. Player login lives at `/o/<slug>/player` and only unlocks the player's own write actions — the open-by-default public pages are untouched. A logged-in player *does* bypass the PI-27 public-password lock for their own org (HTTP `public.ts` preHandler + `realtime.ts` room authorizer both updated).
- [x] **Invites:** organizer picks a roster player + supplies an email on the Roster page → `PlayerInvite` (7-day, hashed token, mirrors `OrganizerInvite`); link is emailed if SMTP is configured and **always returned** so it works without email. Accept sets the password and logs in.
- [x] **Portal (`/o/<slug>/player`):** check in / out of any of the org's tournaments (`TournamentPlayer` upsert/delete; check-out blocked with `already_entered` once the organizer has paired the player into a pod), and report your own match results. Score is framed "you – opponent"; result derived server-side, clamped to the pod's match format, round must be `ACTIVE`. Reuses the shared `Stepper` (extracted from `PairingsPage`). Live via a small `usePlayerPortalRealtime` room subscription.
- [x] **Confirmation model (v1):** either player on a match may submit or overwrite the score while the round is `ACTIVE`; the organizer keeps full edit rights and is the final authority on round completion. No separate confirm step. A "player disputes opponent's entry" flag is a v2 refinement.
- [x] **Revoke:** organizer clears the credentials + bumps `authVersion`, killing any live player session on its next request (same mechanism as an OIDC relink).
- [x] **Tests:** `server/src/services/playerAccounts.test.ts` (invite→accept→login, expired/single-use token, already-has-account guard, revoke, and result-submission authz incl. team members / non-participants / non-ACTIVE round / format clamp); `realtime.test.ts` gains a player-session-authorizes-locked-room case.
- [x] Browser-verified.

### PI-53 — Better match-result entry UI (+/- steppers instead of two text boxes) ✅
Idea from Tobias: entering a match result today is two free-text number inputs (games won per side). Replace with a +/- stepper per player for faster, less error-prone entry on a 0–2 range.
- [x] New `Stepper` component (`PairingsPage.tsx`) — `[−] value [+]`, the number itself stays a real, still-typable `<input type="number">` between the buttons rather than a locked display, so tap-only entry works at the table but direct typing/correction still works too.
- [x] **Clamped to the pod's actual match format:** `gamesWonA`/`gamesWonB` cap at 1 for BO1, 2 for BO3 (`matchFormat` threaded down through `RoundCard` → `MatchCard` → `ResultEntry` from `pod.matchFormat`) — the +/- buttons disable at the bound instead of allowing an impossible score. `gamesDrawn` stays uncapped (min 0 only), same as before.
- [x] Replaces all three number inputs (A wins, B wins, draws) in both the initial-entry and "Edit" re-entry forms.
- [x] Browser-verified (2026-09-02).

### PI-54 — Auto-stop the prep timer when round 1 starts ✅
Idea from Tobias: PI-33's pod prep timer (draft/deckbuilding countdown) has to be stopped manually by the organizer. When round 1 is actually started, the prep timer should clear itself automatically instead of continuing to count down alongside the round timer.
- [x] `POST /api/rounds/:id/start` (`rounds.ts`) now clears `Pod.prepTimerEndsAt`/`prepTimerLabel` when the round being started is round 1 and a prep timer is still set, reusing the same `prep-timer-updated` broadcast PI-33's manual stop button uses — every connected client (organizer, public, Display Mode) picks it up live without any client-side change needed.

### PI-55 — Move round-control buttons above the rounds list ✅
Idea from Tobias: the "Generate pairings"/"Start round" buttons currently render below the rounds list, but newest rounds render at the top of that list — so as a pod progresses, the primary action buttons end up further down the page instead of staying next to the round they act on. Move the buttons above the list.
- [x] `PairingsPage.tsx`: the "Pair round N" / manual-pairing form / "all rounds complete" block now renders above the reversed rounds list instead of below it — purely a JSX reorder, no logic changes.

### PI-56 — Allow reverting a round back to "not yet paired" ✅
Idea from Tobias: once a round is paired there's no way back to the unpaired state, only forward (swap individual pairings). This especially hurts round 1: the draft seating chart (PI-51) only renders once round 1 is paired, but by then the pairing is already locked in behind the swap-only UI. Add an explicit "undo pairing" action for a round that hasn't started yet — deletes its `Match` rows and returns the pod to its pre-pairing state.
- [x] New `DELETE /api/rounds/:id` (`rounds.ts`), org-scoped via `findOwnedRound` — 400s `round_already_started` unless the round is still `PENDING`, otherwise deletes its `Match` rows then the `Round` row and broadcasts a new `round-unpaired` pod event (added to the client's `POD_EVENTS` realtime whitelist).
- [x] Frontend: "Undo pairing" ghost button next to "Start round" on a `PENDING` round (`PairingsPage.tsx`), confirmed via the same native `confirm()` pattern used for other destructive actions (delete tournament/pod/webhook) in this codebase. `useUnpairRound` hook + `round_already_started` error mapping in `useRounds.ts`.
- [x] Not pod-1-specific — works for any not-yet-started round, since the same "back out and re-pair" need can come up on a later round too, not just round 1.
- [x] Browser-verified (2026-09-02).

### PI-57 — Rename remaining "Weekend overview" copy to "Tournament overview" ✅
PI-30 renamed the Gesamtwertung page and the public tournament page, but missed two spots: `TournamentPage.tsx`'s `Eyebrow` label (still hardcoded "Weekend overview") and `TournamentValuePage.tsx`'s fallback title. Same rationale as PI-30 — not every tournament spans a weekend.
- [x] Both strings changed to "Tournament overview"; confirmed via repo-wide grep that no "Weekend overview" text remains anywhere in `client/src` or `server/src`.

### PI-58 — Pod status label should reflect actual progress ✅
Idea from Tobias: the tournament overview shows every pod's status as "SETUP" regardless of how far it's actually progressed. Root cause: `Pod.status` is a manually-set field the app never transitions automatically (per `rounds.ts`'s own comment: "the app doesn't manage pod.status automatically") — only the one-time `import-legacy.ts` script sets it.
- [x] **Derived, not stored** (same posture as PI-51's seating chart) — left the stored `Pod.status` field and its enum untouched (still round-trips through export/import unchanged) and instead compute a display-only label from real round data. `podProgressStatus()` (`client/src/features/pods/usePods.ts`): no rounds yet → "Setup"; the pod's last configured round (`roundNumber === roundCount`) is `COMPLETED` → "Finished"; anything paired in between → "In progress".
- [x] Both tournament-detail queries (`GET /api/tournaments/:id` in `tournaments.ts`, `GET /api/public/o/:slug/tournaments/:id` in `public.ts`) now include each pod's `rounds: { roundNumber, status }` — just enough to derive the label, not the full Round/Match payload the pairings page needs. New optional `Pod.rounds` field on the shared client type.
- [x] Wired into the two places that rendered raw `pod.status`: `TournamentPage.tsx` (authed) and `PublicTournamentPage.tsx` (public).
- [x] Browser-verified (2026-09-02).

### PI-59 — Bug: match-result row overflows on mobile, hiding the seat-A name ✅
Tobias flagged via a phone screenshot right after PI-53 shipped: the `MatchCard` row (`entrant names` + `ResultEntry`'s steppers/Submit) was a single non-wrapping flex row. On a narrow viewport the steppers/Submit — fixed-width, non-shrinking — squeezed the name column down so hard that seat A's name effectively vanished from the layout (only "Table 1" and "vs Kaiser" remained visible).
- [x] `MatchCard`'s outer row and `ResultEntry`'s form both gained `flex-wrap` (`PairingsPage.tsx`) — the result-entry controls now drop to their own row below the names instead of squeezing them, on any viewport too narrow to fit both.
- [x] **Also dropped the separate "drawn games" stepper** (Tobias, same pass) — a match's `DRAW` result was already inferred purely from `gamesWonA === gamesWonB`, so a manual drawn-games count was redundant UI clutter, not something the result computation ever depended on. Removed the input; the underlying `gamesDrawn` value/state is still preserved and still submitted unchanged (so a match imported with historical drawn-game data isn't silently zeroed out), it's just no longer manually editable through this form. Still shown in the read-only completed-match summary when `> 0`.
- [x] **Follow-up (Tobias, next screenshot):** even wrapped, Submit sat cramped right next to the two steppers on phone width. `ResultEntry`'s form is now a fixed two-row stack — steppers+dash on row one, Submit (+ Cancel when editing) on row two — always, not just as a too-narrow-to-fit fallback, so the steppers get consistent breathing room on every screen size.
- [x] **Second follow-up (Tobias):** bigger tap targets and full-width fill. `Stepper` gained a `className` passthrough so its two instances take `flex-1` and stretch to fill the steppers row evenly; the +/- buttons' padding grew (`px-1.5 py-1` → `px-3.5 py-2.5`, text bumped to `text-base`) and the number input is now `flex-1` (was a fixed `w-8`) so it grows with the stepper. Submit is `flex-1` in its own row so it fills the full row width when not editing (shares it with the still-`shrink-0` Cancel link when editing).
- [x] **Third follow-up (Tobias, next screenshot) — root-caused the real problem:** the number had gone fully invisible in the steppers, and one card (short entrant names) rendered with the result-entry controls confusingly interleaved with the name line. Root cause: `MatchCard`'s outer row was still `flex-wrap`, so it only stacked name-vs-ResultEntry when the combined content didn't fit on one line — a short name let both sit side-by-side, and `items-center` then vertically centered the single-line name against the now two-row-tall `ResultEntry`, making it look interleaved. Fixed by making `MatchCard` always stack (`flex flex-col`, no more conditional wrap) so `ResultEntry` unconditionally gets the card's full width — same layout on every card regardless of name length. The stepper's number input also gained a real `min-w-10` floor (replacing `w-0 min-w-0`, which let it get squeezed to zero) so it can never shrink below legible again.
- [x] Browser-verified (2026-09-02).

### PI-60 — Tournament standings table should exclude players who played zero pods ✅
Idea from Tobias: the per-tournament ratings/standings table ("Tournament Standings", formerly Gesamtwertung) should only list players who took part in at least one pod.
- **Current behavior (confirmed):** `computeGesamtwertung` (`server/src/services/gesamtwertung.ts`, lines 33–68) seeds its row map from **every `TournamentPlayer` row** (everyone registered/attending), not just those who played — `eventsPlayed` starts at 0 for all of them and the pod loop only increments counters for actual entrants, never removes anyone. `playerIds` (line 68) is built from that full map with no `eventsPlayed > 0` filter, so a registered-but-never-played attendee shows up as a "0 of N pods played" row (`client/src/components/GesamtwertungList.tsx` line 44 renders exactly that string).
- **For contrast:** the org-wide Hall of Fame (`server/src/services/hallOfFame.ts`) already gets this right *by construction* — it seeds its player map only from pod entrants (lines 40–55), so a zero-pod attendee never appears there at all. PI-60 is about bringing the per-tournament table in line with that.
- [x] `playerIds` in `computeGesamtwertung` now filters to `eventsPlayed.get(id) > 0` before building rows. `GesamtwertungList.tsx`'s empty-state copy updated from "No players attending yet." to "No one has played a pod yet." to match (a registered-but-not-yet-played tournament now correctly shows the empty state instead of a wall of "0 of N" rows).
- [x] **Follow-up (Tobias, same root cause, different spot):** the tournament overview page's "N pods · M players attending" summary had the exact same bug — `M` came from `tournament.players.length` (raw `TournamentPlayer` registrations), so a fully-registered-but-not-yet-played tournament could read "13 players attending" with zero pods played. New shared `countTournamentParticipants()` (`gesamtwertung.ts`) counts distinct players who actually appear as a pod entrant; both tournament-detail routes (`tournaments.ts` authed, `public.ts` public) now fetch each pod's entrants alongside its rounds, compute `playersPlayed` from them, and strip the raw entrant list back out of the response before sending (keeps the wire payload the same shape as before, just adds one number). Wired into both `TournamentPage.tsx` and `PublicTournamentPage.tsx` — copy changed from "N players attending" to "N players played", and the "View standings"/"Best pulls" links now gate on `playersPlayed > 0` instead of the raw registration count.
- [x] Browser-verified (2026-09-02).

### PI-61 — Hall of Fame "Victim"/"Nemesis" should ignore opponents with only 1 tournament played ✅
Idea from Tobias: a player's "Victim" field on their Hall of Fame / player-detail page (their most-beaten opponent) shouldn't be someone who's only played in a single tournament — too small a sample to fairly brand them the group's punching bag. Decided: apply the same fix to "Nemesis", falling back to the next qualifying candidate rather than going to `null`.
- **Root cause (confirmed):** `computePlayerStats` (`server/src/services/playerStats.ts`) picked `victim`/`nemesis` as the single best/worst head-to-head win% among opponents with `matches >= MIN_HEAD_TO_HEAD_GAMES` (2) — a raw match-count gate only, with no check on how many separate tournaments the candidate has played overall.
- [x] New `MIN_TOURNAMENTS_FOR_H2H_AWARD = 2` constant. Built `tournamentsPlayedById` from the `hallOfFame` data already fetched in this function (no extra query) and added an `eligibleForAward` filter on top of the existing match-count `qualifying` filter — a candidate below the tournament-count bar is excluded from consideration entirely, so `nemesis`/`victim` are picked from whoever's left. Because ineligible candidates are dropped rather than deprioritized, this **naturally falls back to the next-best qualifying candidate** (Tobias's decided behavior) without any extra fallback logic — `null` only when nobody clears both bars.
- [x] Browser-verified (2026-09-02).

### PI-63 — Drop a player mid-tournament ✅ (shipped in v0.3.1)
Idea from Tobias: a player might drop from a running pod partway through the weekend; the app needs a way to record that so pairing stops matching them against anyone for the rest of the pod.
- [x] **Confirmed the pairing engine needed zero new logic.** `Entrant.droppedAfterRound` and `getActiveEntrants()` (`server/src/services/pairing.ts`) already excluded a dropped entrant from every round after the drop, and the existing lowest-score/no-prior-bye rule in `generatePairings` already assigns the resulting odd-pool bye correctly — this item was purely "give organizers a way to set the field."
- [x] **Backend:** `POST /api/entrants/:id/drop` / `POST /api/entrants/:id/undrop` (`server/src/routes/pods.ts`), both gated by a new shared `getLatestRound(podId)` (`pairing.ts`, also now reused by `rounds.ts`'s `checkNextRoundAllowed`) — 400 `round_in_progress` if the pod's latest round exists and isn't `COMPLETED`. `drop` sets `droppedAfterRound = latest?.roundNumber ?? 0` (409 `already_dropped` if already set); `undrop` clears it to `null` (409 `not_dropped` if it wasn't set). No realtime emit, matching the existing convention that entrant add/remove/tiebreak don't emit either. Schema comment added on `Entrant.droppedAfterRound` documenting the exact semantics.
- [x] **Frontend:** `useDropEntrant`/`useUndropEntrant` (`useEntrants.ts`), a shared `EntrantDropControl` (`PodPage.tsx`) rendering a "Dropped" badge + Drop/Un-drop button in both the individual and team entrant lists, gated by a `canModifyRoster` flag computed the same way `PairingsPage`'s `canGenerateNext` already does (`rounds.length === 0 || lastRound.status === "COMPLETED"`) — disabled with an explanatory `title` when a round is open. No changes needed to `PairingsPage`, standings, or public pages: dropped entrants were already correctly excluded from pairing pools and already keep their earned standings once the field is actually set.
- [x] **MCP:** `drop_entrant` / `undrop_entrant` added as plain (non-destructive, reversible) tools next to `add_individual_entrant`.
- [x] **Test:** `pairing.test.ts` — completes round 1 of a 4-entrant pod, drops one entrant, asserts `getActiveEntrants` excludes them and `generatePairings` never pairs them in round 2.
- [x] Browser-verified by Tobias; released as v0.3.1 (2026-09-01).

### PI-64 — Add pod entrants via a roster checklist modal ✅
Idea from Tobias: adding players to an individual pod was a one-at-a-time dropdown.
- [x] `POST /api/pods/:id/entrants` (individual pods) now also accepts a bulk form — `{ playerIds?: string[], newPlayerNames?: string[] }` — adding everyone in one transaction (existing already-in-pod ids are silently skipped, not a 409). The legacy `{ playerId }` single form is untouched (MCP `add_individual_entrant`, back-compat). New `bulkEntrantSchema` + a branch at the top of the handler; team pods unchanged.
- [x] Frontend: `EntrantPickerModal.tsx` (on the shared `Modal`) — full-roster checklist with already-entered players ticked + disabled, a **Select all / Clear all** toggle, and error surfacing. Replaces the `<select>` + form in `IndividualEntrants` (`PodPage.tsx`) with a **+ Add players** button. New `useAddEntrantsBulk` hook invalidates both `["pods", id]` and `["players"]`.
- [x] Browser-verified. No dedicated test (pods.ts has no route-test harness) — covered by build + browser check.

### PI-65 — "Add a new player" from inside the pod entrant modal ✅
- [x] The `EntrantPickerModal` has a **New player** field: type a name → "Add" appends it as a removable chip. On confirm, `newPlayerNames` goes to the bulk endpoint, which creates each `Player` on the roster **and** adds them to the pod in the same transaction. Case-insensitive collision (against the roster or another new name in the same request) is rejected with `409 name_taken` + the offending name, surfaced as "tick them in the list instead". Client also pre-checks against the loaded roster.
- [x] Browser-verified.

### PI-66 — Per-pod "rare picks" toggle (disable value tracking for a pod) ✅
Idea from Tobias: a per-pod switch to turn card-value tracking off entirely.
- [x] `Pod.rarePicksEnabled Boolean @default(true)` (migration `20260903120000`). Default on = unchanged behaviour. Distinct from `excludeFromStats` (which only touches standings/HoF aggregates, still lets pulls be added, and doesn't hide the tab). Existing pulls are **kept** when it's turned off and reappear if turned back on.
- [x] **When off:** `POST /api/pods/:id/card-pulls` → `409 rare_picks_disabled`; the pod is filtered out of every value rollup — per-tournament Best Pulls (`cardPulls.ts` + `public.ts`), org Treasure Chest (both), and the Hall-of-Fame value stats (`playerStats.ts` — 3 queries) + the export's Treasure Vault snapshot (`orgExport.ts`). The flag round-trips through export/import (`orgExport.ts` / `orgImport.ts`, the import field defaults to `true` so pre-PI-66 files still load).
- [x] **Frontend:** a checkbox in `EditPodForm` (`PodPage.tsx`). `PodTabs.tsx` drops the Value tab when off (reads the cached `usePod`). `PodValuePage` shows an explanatory note instead of the gallery/add form. `PublicPodPage` hides its Value `<section>`.
- [x] Browser-verified. No dedicated test (cardPulls/playerStats have no route/service test files) — covered by build + the orgImport round-trip test + browser check.

### PI-67 — Show finishing place as a column in the standings table ✅
Idea from Tobias: the per-pod standings table didn't show the rank number.
- [x] Added a leftmost `#` column (sequential `i + 1`, muted, right-aligned tabular) to the per-pod standings table on both `PodStandingsPage.tsx` (authed) and `PublicPodPage.tsx` (public). No backend change — rows already arrive pre-sorted from `computePodStandings`. The tournament-wide standings (`GesamtwertungList.tsx`) already had a tie-aware rank badge, so it needed nothing. Sequential (not tie-aware) per the v1 scope — the manual-reorder arrows already handle the intentional-tie case.
- [x] Browser-verified.

### PI-68 — Export a tournament as a spreadsheet (.xlsx) ✅
Idea from Tobias: a human-readable spreadsheet export of a tournament, distinct from PI-38's JSON round-trip dump.
- [x] **Decisions (Tobias):** `.xlsx` multi-sheet; contents = Tournament Standings + a Standings sheet per pod + one flat Matches sheet (round-by-round); download button on the tournament page.
- [x] **Dependency:** `write-excel-file` (chosen over `exceljs` — `exceljs` pulls transitive advisories in its CSV-read path we don't use; `write-excel-file` audits clean and is write-only, which is all we need).
- [x] `server/src/services/tournamentSpreadsheet.ts` — `buildTournamentWorkbook(id)` reuses `computePodStandings` + `computeGesamtwertung`, derives W/L/D from Match rows (a bye counts as a win), tie-aware rank on the Gesamtwertung sheet, sanitises sheet names to Excel's 31-char / no-`[]:*?/\` rule. Route `GET /api/tournaments/:id/export.xlsx` (`tournaments.ts`, org-scoped). Frontend `useExportTournamentXlsx` hook (direct fetch → Blob download, same as the org export) + an "Export spreadsheet ↓" button on `TournamentPage`. A **Tokens** sheet is added when the org has PI-72 tokens on.
- [x] Smoke-tested end to end against a real Postgres — valid xlsx, correct sheets, bye/pending/draw and dirty pod names all handled.
- [x] Browser-verified (download + open in Excel/LibreOffice).

### PI-69 — Drop the dead `Pod.packConfig` / `Pod.rarepicUrl` columns ✅
Both fields were in `schema.prisma`, `client/src/lib/types.ts`, and both API schemas (`podCreateSchema` / `podUpdateSchema` in `pods.ts`), but nothing read or set them — no UI anywhere, early-design leftovers. Confirmed via a full-repo grep that `orgExport.ts`/`orgImport.ts`/`import-legacy.ts`/the `mcp/` package didn't reference them either — only those three files did.
- [x] **Built:** dropped from `schema.prisma`, both Zod schemas in `pods.ts`, and the client `Pod` type. New migration `20260905150000_drop_dead_pod_columns` (`DROP COLUMN` ×2). No user-facing effect.
- [x] Full server suite 134/134 (against the real Postgres test DB, migration applied and columns actually gone). `tsc -b` (client + server) and a real `vite build` all clean.

### PI-70 — Hide the pre-round timer once a pod is under way ✅
Follow-up to PI-33/PI-54: PI-54 auto-*clears* a running prep timer when round 1 starts, but the `PrepTimer` control on `PodPage` then fell back to showing the "start a pre-round timer" setup form for the rest of the tournament, which makes no sense mid-event or on a finished pod.
- [x] `PodPage.tsx` — `<PrepTimer>` now renders only while `!podUnderway` (no round is `ACTIVE`/`COMPLETED`). The read-only `PrepTimerDisplay` on the Pairings tab / public page is unchanged (it already returns null when no timer is set).
- [x] Browser-verified.

### PI-71 — Close the new-pod form when the pod is created ✅
Idea from Tobias: creating a pod left the (long) new-pod form open with no clear signal it worked — easy to miss the new pod if it landed below the fold.
- [x] `NewPodForm` (`TournamentPage.tsx`) takes an `onCreated` callback, fired from `createPod.mutate(..., { onSuccess })`; the parent closes the form. The new pod renders directly above where the form was, so no scroll-into-view needed.
- [x] Browser-verified.

### PI-72 — Tokens: an org-wide player currency earned from play ✅
Idea from Tobias: players accumulate **tokens** across every event, redeemable for prizes (the prize wall itself is **out of scope** — this app only tracks the balance). A new concept, deliberately named **"tokens"** to stay distinct from match/standings points.

**Spec + decisions (Tobias, 2026-09-02):**
- Players earn tokens for **playing in a pod** (participation) and a **standing bonus** by finishing place.
- Tokens accumulate **org-wide, all-time** — one balance per player across all tournaments.
- Organizers can **set** a balance (import external history), **add**, and **deduct** (prize-wall spend).
- Every transaction shows on a **new "Tokens" tab on the player stats page**: delta, date, reason — "manual, by \<organizer\>" or "automatic, from \<pod\>".
- **Config:** `Tournament` holds the default participation value (single int) + a **configurable place→bonus list** (rows like place 1 = 5, place 2 = 3, places 3–4 = 1). Each `Pod` can **override** both (null = inherit the tournament default).
- **Standing-bonus shape:** a list of `{ fromPlace, toPlace, tokens }` rows, stored as JSON on `Tournament` + `Pod`.
- **Auto-award + recompute:** awarded when a pod's last configured round is `COMPLETED`, from the standings at that moment; **recomputed** (delete + re-create the pod's auto entries) whenever a later result correction shifts those standings — hooked next to `inferCardPullAttribution(podId)` in `rounds.ts`. Team pods credit **each member** the full participation + standing tokens (mirrors the match-points rule).
- **Visibility:** current **balance is public** on the player page; the **transaction ledger is private** — visible to organizers, and to the player themselves when logged into their own PI-52 player account (also surfaced in the `/o/<slug>/player` portal).
- **Data model:** a `TokenTransaction` ledger — `playerId`, `orgId`, signed `delta`, `reason` enum (`POD_PARTICIPATION` / `POD_STANDING` / `MANUAL` / `INITIAL`), nullable `createdByOrganizerId` / `podId`, optional `note`, `createdAt`. Balance = `SUM(delta)`. "Set to X" is stored as the adjusting delta with reason `MANUAL` + a note.
- **Retroactivity:** automatic awards start when the feature ships; pre-existing external history goes in via the manual "set balance".
- **MCP:** `adjust_player_tokens` (delta + note) and `list_player_token_transactions`.
- [x] **Built:** `Organization.tokensEnabled` + `Tournament`/`Pod` token config (JSONB bonus list) + `TokenTransaction` ledger (migration `20260903150000`). `server/src/services/tokens.ts` — `syncPodTokenAwards` (idempotent reconciler, hooked next to `inferCardPullAttribution` in every handler that moves a pod's standings/completion — `rounds.ts` × 3, `playerAccounts.ts`, pod entrant add/remove/drop/undrop, pod update, tournament update, and the settings toggle), `recordManualTokenTxn`, balance/ledger getters. Enabling the feature backfills every completed pod.
- [x] **Routes:** `PUT /api/settings/tokens`; `GET /api/players/:id/token-ledger` + `POST /api/players/:id/token-adjust` (organizer); `GET /api/player/tokens` (PI-52 portal). `computePlayerStats` → `tokenBalance: number | null` (null hides everything). `/auth/me` → `tokensEnabled`.
- [x] **Frontend:** `TokensSection` in Settings; a `#`-style tab bar on `PlayerStatsPage` (Overview | Tokens) when on; `PlayerTokenLedger` (balance + add/deduct/set form + table), reused read-only in the player portal; balance stat card in `PlayerStatsBody` (public too); `StandingBonusEditor` on the tournament + pod edit forms, all gated on `useMe().tokensEnabled`.
- [x] **MCP:** `adjust_player_tokens` + `list_player_token_transactions`. **Tests:** `server/src/services/tokens.test.ts` (11 — bonus lookup, config inherit/override, awards + team expansion + recompute + un-complete removal + disabled-org no-op, manual delta/set + disabled rejection).
- [x] **Export / import (PI-38/39):** the `data` section now carries `tokensEnabled`, the tournament + pod token config, and the **hand-made** ledger rows (`MANUAL`/`INITIAL`, keyed by player displayName + `createdAt`, deduped on re-import so it's idempotent). The `POD_PARTICIPATION`/`POD_STANDING` rows aren't exported — `importOrgData` re-runs `syncPodTokenAwards` for every imported pod after the transaction to regenerate them. Import only ever *enables* tokens on the target org, never disables. `createdById` (organizer) isn't round-tripped — imported rows have it null, the note is preserved. Round-trip test in `orgImport.test.ts`.
- [x] **Excel export (PI-68):** a **Tokens** sheet — one row per player who earned tokens from this tournament's pods (Player · earned here · current org-wide balance), only when the org has tokens on.
- [x] Browser-verified.

### PI-73 — SSO-only accept-invite page ✅
On deployments with `LOCAL_LOGIN_DISABLED=true`, the `/accept-invite?token=…` page showed a name + password form — unusable because local login is disabled. The invitee had no way to accept the invite.

Fix: the backend's `linkOrProvisionFromSso` already handles invite-by-email (path 3 in the sso.ts comment — it finds any valid pending invite matching the SSO email and provisions the account into that org). The frontend just wasn't aware: `AcceptInvitePage` now checks `appConfig.localLoginDisabled` and, when true, replaces the password form with SSO buttons + a note telling the invitee to use the email address the invite was sent to.
- [x] **Built:** `AcceptInvitePage` branches on `localLoginDisabled` — SSO-only mode shows `SsoButtons` with invite context; no backend changes required.
- [x] Browser-verified.

### PI-74 — Copy invite link + QR code modal (SMTP-free invite flow) ✅
On deployments without SMTP configured, organizer invites failed entirely (`503 email_not_configured`) and player invite links were shown in a plain inline text field with no QR code.

The fix makes invite link delivery SMTP-optional and surfaces the link via the existing `SharePopup` (copy + QR code modal):
- **Organizer invites:** the `POST /api/settings/organizers/invite` endpoint no longer requires SMTP — it always creates the token and returns `{ link, emailSent }`, sending mail only when SMTP is configured. The `OrganizersSection` shows a `SharePopup` after a successful invite (new or re-invite). Pending invites in the list gain a **Copy link** button that re-invites the same email (superseding the old token per the existing behavior) and opens the SharePopup.
- **Player invites:** `RosterPage` upgrades the post-invite inline link display to a `SharePopup` with QR code. `SharePopup` gains an optional `url` prop for callers that already have an absolute URL (player invite links are built server-side).
- [x] **Built:** `settings.ts` invite endpoint; `useOrganizers.ts` return type; `OrganizersSection`; `SharePopup` (`url` prop); `RosterPage` player invite upgrade.
- [x] Browser-verified.
